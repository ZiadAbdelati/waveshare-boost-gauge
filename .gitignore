build/
sim/build/
!release/
sdkconfig
sdkconfig.old
dependencies.lock
managed_components/
.vscode/
.idea/
*.pyc
__pycache__/
.DS_Store
preview/sim/*.raw
preview/sim/frames/
android/.gradle/
android/.kotlin/
android/**/build/
android/local.properties
main/boost_wifi_secrets.h
.orca/
IMG_*-ezgif*.gif
preview/regression-repair/
preview/text-gif-repair/

# Ad-hoc render scratch from panel/sim iteration (regenerate with sim/ or the
# debug snapshot endpoint; the committed reference set is preview/sim/).
preview/panel/
preview/sim_*/

# Per-agent build trees
build*/

# Neon theme mockups: the four contact sheets are the committed design
# reference. Per-state frames are regenerable with tools/neon_mockup.py.
preview/neon_mock/*
!preview/neon_mock/*_sheet.png
# Session render scratch (regenerate with sim/ --screenshot; committed set is preview/sim/)
preview/mq_*/
preview/device_*.png
preview/device_*.raw
.pi-subagents/
