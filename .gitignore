build/
sim/build/
!release/
sdkconfig
sdkconfig.old
dependencies.lock
managed_components/
.vscode/
.idea/
*.pyc
__pycache__/
.DS_Store
preview/sim/*.raw
preview/sim/frames/
preview/sim/*/*.raw
preview/sim/*/frames/
android/.gradle/
android/.kotlin/
android/**/build/
android/local.properties
main/boost_wifi_secrets.h
.orca/
IMG_*-ezgif*.gif
preview/regression-repair/
preview/text-gif-repair/

# Ad-hoc render scratch from panel/sim iteration (regenerate with sim/ or the
# debug snapshot endpoint; the committed reference set is preview/sim/).
preview/panel/
preview/sim_*/

# Per-agent build trees
build*/

# Session render scratch (regenerate with sim/ --screenshot; committed set is preview/sim/)
preview/mq_*/
preview/device_*.png
preview/device_*.raw
preview/neon_*/
preview/doto*/
preview/doto*.png
.pi-subagents/

# Bench capture outputs (reproducible with tools/bench_fast_motion.py and
# tools/bench_theme_matrix.py; the summaries live in AGENTS.md). Keeping them
# tracked made a plain `git status`/branch diff show +10k lines of telemetry.
crossings-*.json
fast_motion_sweep_results.json
fast_motion_organic_results.json
baselines/
after/
*.o
.claude/
deepseek-neon-review.*
.build/
.tmp/
