# Agent guidance

This repository is an ESP-IDF 5.5.1 firmware/dashboard for an ESP32-S3 AMOLED boost gauge. These rules are load-bearing. A fresh agent must be able to resume from this file alone; do not rely on chat history.
Verified release baseline is firmware `0.3.0-web`; preserve that identity in hardware and release notes for current measurements. Historic rows in the regression ledger retain the version-independent measurements observed when each guard was established.

## Durable coordination and delegation

- For every non-trivial change, the coordinator MUST use `Task` subagents heavily: delegate independent research, implementation slices, and verification/testing slices in parallel where possible. The coordinator owns the top-level contract, integration, and final acceptance; do not delegate away the architecture decision.
- Before assigning work, map the affected files and symbols. Announce ownership through the coordination channel. One agent owns a file at a time; agents MUST NOT overwrite unexpected work. Re-read after another agent edits, and integrate only the intended diff.
- Every subagent must return observed evidence, decisions, files touched, and remaining risks. Continuously append durable findings/decisions/architecture notes to this `AGENTS.md` and the relevant `README.md` section while work is in progress; do not keep critical reasoning only in the session. Whenever architecture or a regression changes, update both the README and the AGENTS ledger in the same change.
- No unverified edit is acceptable. First inspect the existing implementation and callers, then make the smallest source change, then exercise the affected path. Never invent measurements, claim a test was run when it was not, or replace a failing guard with a weaker one.
- Do not edit generated C assets or release binaries by hand. Do not add compatibility shims, alternate implementations, or a second convention beside an existing one.

### Source ownership

- `main/boost_display.c/.h`: AMOLED bring-up, LVGL/DMA buffers, panel transfer and display lock.
- `main/boost_gauge.c/.h`: gauge rendering and exclusive GIF playback lifecycle.
- `main/boost_media_store.c/.h`: raw `media` partition format, upload transaction, CRC, mapping, and deletion.
- `main/boost_web.c`: HTTP/WebSocket API and upload/delete request serialization.
- `main/boost_model.c/.h`: sensor/model state and publication.
- `web/`: dashboard source (cockpit + settings views). `main/generated_web_assets.c/.h` are generated outputs only.
- `tools/embed_web.py`, `web.mk`: web asset regeneration. `tools/mock_server.py` mirrors config/network APIs. `release/`: explicitly produced release artifacts only.

### Theme controls and physical input

`boost_theme.c:s_defaults[]` is the single authoritative theme order. The web
picker and physical swipes consume that order through `/api/v1/themes` and
`boost_theme_at()`. The selectable order is Dyno Cell, Vault-Tec, Night City,
and Big Digit. Sport Cluster was removed outright (renderer, enum member,
`"sport"` style token and web mirror) in the 2026-08-10 repo audit; its design
history lives in git if it is ever revived. A vertical swipe up advances to
the next entry; swipe down moves to the previous entry. The screen requires 48 px of predominantly
vertical travel, preserving short-tap peak reset and the one-second brightness
hold. Theme changes rebuild one LVGL scene under the existing display lock; no
full-frame transition buffer or animation is used because it would threaten
the 16 ms partial-refresh path. If Sport Cluster is restored later, keep its static ring/ticks in the PSRAM
`lv_canvas` and its live readout bounded rather than repainting the full face.
Vault needle color and counterweight tail are persisted theme settings. Red
recolors only the body, the hub remains green, and changing either setting must
rebuild or invalidate the complete old/new needle geometry.

`boost_page.c/.h` owns page 0 (boost) and page 1 (TPMS): LEFT enters TPMS and
RIGHT returns to boost, with no wrap-around. Vertical theme swipes and tap peak
reset are page-0-only; the one-second brightness hold works on either page.
TPMS BLE central remains feature-flagged off by default (`BOOST_TPMS_BLE_ENABLED=0`)
until a verified vLinker FD+ BLE GATT profile exists.

Gesture classification tracks the greatest signed excursion during
`LV_EVENT_PRESSING`, not only the release coordinate. Only movement within the
12 px tap slop resets peak; a meaningful horizontal/ambiguous drag is rejected,
and a valid vertical drag changes one theme at 48 px. GIF playback suppresses the
tap-reset, theme-swipes and page switches, but the one-second hold-to-dim still
works over media (the press is tracked while the GIF owns the screen; only
tap/theme/page actions are gated on `media_active()`). Movement from
12 through 47 px is a rejected drag rather than a peak reset. The screen object
survives theme rebuilds, so synchronous
`boost_gauge_apply_theme()` from `LV_EVENT_RELEASED` deletes children but not
the event target and does not require an unverified async callback.

## Cadence contract

Keep these rates distinct; never use one as a substitute for another:

- Sensor sampling and the physical gauge render/update path: every **16 ms**, approximately **60 Hz** (the physical gauge is the hardware gate).
- **Every selectable theme has two cadence gates.** The constant-slew fast-motion sweep (`tools/bench_fast_motion.py sweep --theme <id> [--layout N]`, 9.789 psi/s) is the direct capacity gate: each theme (dyno-cell, vault-tec, night-city, big-digit, neon tube/segments/marquee) must sustain median physical `renderFps` ≥60; dyno-cell is the reference guard, not a ceiling. The organic demo waveform is demand-aware: `tools/bench_theme_matrix.py` compares `renderFps` with `gaugeDemandPerSecond`, one count per 16 ms gauge update that creates at least one dirty area. PASS requires median demand coverage ≥95% in demanded windows; zero-demand windows are idle, not failures. Firmware without the metric retains the legacy median-60 gate. A real MAP sensor at constant atmosphere invalidates nothing and is not a cadence measurement; demo mode is the precondition for every check. This is a measurement correction, never a throttle. No visual compromise buys frames: a visual-vs-performance trade is a proposal to the user first.
- Network telemetry: a fixed pool of **3 WebSocket clients**, each with at most one in-flight heap-owned frame; bounded broadcast is notification-driven at the sample rate, **62.5 Hz** (`STATE_WS_PUSH_DECIMATION 1`, one frame per 16 ms sample). `STATE_WS_FRAME_MS 50` is the idle *fallback* timeout for a stalled producer, not the period.
- Browser application heartbeat: **750 ms**, consumed by the server.
- Browser live canvas: renders on every `requestAnimationFrame`, uses a **35 ms** EMA, and accepts only strictly newer `uptimeMs` targets; timing resets after a gap greater than **1 s**.
- HTTP fallback state polling: **4 Hz**.
- Sparkline: **4 Hz**.
- Calibration diagnostics (`GET /api/v1/sensors/calibration`): **2 Hz** while the Settings panel is visible, **1 Hz** on the cockpit for the Night City ATM readout. Deliberately off `/state` and the WebSocket so the 62.5 Hz payload and its smaller JSON buffer are untouched, and so Settings can show real sensor state while demo mode drives the gauge. The cockpit ages the reported `bmpAgeMs` against the browser clock between polls, so a stalled sensor falls back to `ATM --kPa` on schedule rather than at the next poll.
- Background RAM logging: **12.5 Hz** (every fifth 16 ms model sample); 1,800 entries retain **2 minutes 24 seconds**.
- Browser connection badge: **Live · WebSocket 60 Hz**, **Live · HTTP 4 Hz**, or **Disconnected**; it MUST expose the active transport.
- Browser device-pixel ratio: cap at **2**.
- Browser GIF preview: disabled; do not reintroduce a large client-side preview path.

Do not add display timers/dividers, throttle the 16 ms gauge readout, or judge WebSocket/canvas cadence with the physical-display guard. Demand-aware organic acceptance is a measurement correction (`renderFps` versus `gaugeDemandPerSecond`), never a display divider or render throttle. GIF playback is an exclusive full-frame path and is not expected to satisfy the live-gauge FPS threshold. A new dashboard MUST NOT evict an existing WebSocket client: single-owner behavior caused concurrent/stale tabs to force Live/Fallback churn and out-of-order target jitter.

### Fourth-client and GIF regression invariants

- The WebSocket pool is exactly three clients. A fourth handshake may be
  rejected/closed for the newcomer, but MUST NOT close or disturb any existing
  client. Browser fallback remains `POLL_FRAME_MS=250` (4 Hz), while WebSocket
  retries are 1 s. A successful HTTP state sample MUST show `Live · HTTP 4 Hz`;
  a retry attempt MUST NOT mark it `Disconnected`. Restored WebSocket delivery
  MUST show `Live · WebSocket 60 Hz`. `Disconnected` requires both transports to fail.
- GIF decompression is not project-written. `main/boost_gauge.c` supplies the
  custom locked widget/descriptor integration; `main/boost_media_store.c` owns
  raw dual-slot CRC publication and `esp_partition_mmap`; LVGL
  `managed_components/lvgl__lvgl/src/libs/gif/lv_gif.c` and bundled
  `AnimatedGIF/src/gif.c` perform parsing, LZW, frame timing, and composition
  (`GIF_openRAM`/`GIF_playFrame`). Local LVGL edits zero the full framebuffer
  and disable turbo (`pTurboBuffer = NULL`) because turbo failed delta/disposal
  composition. Keep mapped bytes alive through widget destruction: lock display,
  destroy widget, then unmap. Do not describe this as a custom decoder.

## AMOLED DMA invariants and hardware gate

- Start the gauge with `boost_display_start()`, never stock `bsp_display_start()`.
- LVGL draw buffers MUST stay in internal DMA-capable memory (`use_psram = false`). Production strips are 20 lines: 18,640 bytes per buffer / 37,280 bytes double-buffered. Keep transfers capped to one strip and queue depth at 4 (`CONFIG_BSP_LCD_TRANS_QUEUE_DEPTH=4`). Preserve the display lock and internal-memory reservation needed for Wi-Fi and DMA to coexist.
- Preserve LVGL partial refresh and the CO5300 even/odd rounder. Do not move full-frame buffers to PSRAM, enlarge strips for an unmeasured speed claim, restore a marker/knob, replace the arc with retained line segments, or invalidate stationary arc endpoints without before/after evidence.
- After any display-path change, flash hardware and run the cadence guard for a 30-second live-gauge soak:
  `python3 tools/check_display_cadence.py --url http://<board-ip> --seconds 30`
  Accept only a sustained median of at least 60 physical FPS; the verified reference check was min 61, median 63 over 112 samples.
  **Run this in demo mode on the `dyno-cell` (arc) face** — the conditions the 60 FPS threshold was established under (measured 2026-07-25 at the 16 ms sensor cadence: arc + demo **min 57, median 60** — pass). dyno-cell is the *reference* guard, not a ceiling. Each theme must also hold sustained median physical `renderFps` ≥60 on the constant-slew fast-motion sweep (`python3 tools/bench_fast_motion.py sweep --theme <id> [--layout N]`, 9.789 psi/s). The organic matrix (`python3 tools/bench_theme_matrix.py`) instead requires median demand coverage ≥95% in demanded windows and excludes zero-demand idle windows; old firmware without `gaugeDemandPerSecond` uses the legacy median-60 fallback. LVGL renders only what is invalidated, so a real MAP sensor at constant atmosphere legitimately reports `renderFps` in the single digits — that is not a cadence measurement, and demo mode is the precondition for every check here. Switch back to the real sensor afterwards. Serial must show the internal-DMA path and no `ESP_ERR_NO_MEM` or `send color data failed`. A hardware result is required before declaring the change complete.

## Raw media is authoritative (never SPIFFS)

The media store MUST remain a raw dual-slot partition; do not reintroduce SPIFFS, a second staging file, or direct replacement of `active.gif`.

- Partition label is `media`, offset `0x820000`, size `0x7E0000`, split into two slots. Boot scans headers and accepts only CRC-valid headers, selecting the newest generation.
- Upload targets the inactive slot. Erase only the required aligned range, stream payload and CRC, and write the committed header last. A header is not publication: commit is atomic only after payload and validation are complete.
- Playback maps the committed payload with the raw partition mmap API and gives LVGL a variable image descriptor. Do not free mapped storage until LVGL has been destroyed/unmapped.
- Abort or any failed upload MUST preserve the previously committed GIF. Delete removes the committed slot only after playback is stopped; repeated deletes must remain harmless (two repeated deletes are verified).
- The verified hardware benchmark is a complete **1,379,129-byte** GIF upload through the raw dual-slot store in **7.504 s**. The earlier SPIFFS history includes approximately **172 s** writes; an earlier tuned SPIFFS result of **7.37 s** was fragile. The raw store is the architectural fix, not an optimization invitation to restore SPIFFS.

### Upload/delete state machine

`Idle → Uploading → Validating/Committing → Published → Playback` is the only publication path. `Uploading` is serialized; an overlapping upload or delete is rejected by the server with **409**. On failure or cancellation, transition through abort/cleanup back to `Idle` while retaining the prior committed slot. In the browser, clicking Delete during upload MUST abort the XHR, wait for its settlement, then issue `DELETE`; never fire concurrent replacement/delete requests. Preserve this ordering and the server-side 409 guard.

## Web assets and release artifacts

- Edit `web/index.html`, `web/app.js`, and `web/styles.css`, then regenerate embedded assets with:
  `python3 tools/embed_web.py web main/generated_web_assets.c main/generated_web_assets.h`
  Review the generated diff; never hand-edit `main/generated_web_assets.c/.h`.
- **Regeneration is not part of the build.** A web edit that is committed, built and flashed without this step ships a dashboard that silently keeps its previous behaviour. This has happened: three consecutive commits of neon web-mirror work reached the board with none of it live, and the symptom looked like a rendering bug in the firmware rather than a stale asset.
- **Verify against the device, and decompress first.** Assets are served gzipped, so grepping the raw HTTP response for a token you just added finds nothing whether the asset is current or stale — that false signal produced a wrong diagnosis before it produced the right one. Check with:
  `python -c "import urllib.request,gzip;r=urllib.request.urlopen('http://<ip>/app.js');print('TOKEN' in gzip.decompress(r.read()).decode())"`
- A release is not complete until the verified ESP-IDF build produces the app image and the release directory contains the bootloader, partition table, OTA data, app image, merged full-flash image, flash helper, and refreshed `SHA256SUMS`. The merged image is for resetting the complete layout; later web OTA uses the app image, not the merged image. Tag the release commit, publish every file in `release/` except documentation-only metadata as GitHub release assets, mark the new release latest, and verify the published asset checksums. Do not publish artifacts from an unverified build or claim hardware behavior from a host-only run.
- Hardware release verification must cover boot, network access, the live physical cadence gate, transport-specific badge behavior, media upload/abort/delete behavior, and serial error absence. Record measured outputs in the README and this ledger before committing.

## Commit hygiene

Keep commits narrow and reviewable: source, generated web output, documentation/ledger, and release artifacts should be separable when practical. Never mix drive-by formatting or unrelated refactors with a regression fix. A commit that changes web sources must include regenerated embedded assets; a commit that changes architecture or a regression must include the README/AGENTS ledger update. Before handoff, report exact files changed, commands actually run, hardware versus host-only evidence, and any unverified risk.

## Chronological regression ledger

| Sequence | Symptom / finding | Root cause | Fix | Guard now required |
|---|---|---|---|---|
| Banded flip sweep for the neon tube was built, measured, then REVERTED on the user's visual review (2026-08-10) | The tube's boost↔overboost zone flip repaints the whole lit run in one frame, measured as a ~90-103 ms flip-window worst cycle (regionDBuf + TE-wait bound, not raster bound). A banded flip sweep (spread the recolor over N frames, the draw painting the run split at a sweep edge so the byte-exact audit stays clean) cut the flip-window worst to ~52 ms (4 bands) / ~69 ms (2 bands). But on glass it read badly: a visible ~70-130 ms orange sweep up the ring at every crossing (2 bands: ~32 ms), the inner/darker halo band appearing to lag the middle band during motion, and the VACUUM↔BOOST flip at the notch racing a tiny boundary across. The user judged it "worst of both worlds" and asked to revert. | The structural flip cost (flush + TE wait of the ~108k px merged region span) cannot be removed by raster work, and any recolor spread is visibly a two-tone transition at the crossing. The 4-band sweep's boundary persisted through fast motion (the tip kept growing while the edge advanced one band per frame), and sweeping a ~1 deg run at the VAC↔BOOST notch read as a colour race. | **Reverted entirely.** The tube keeps the pre-sweep single-gradient-arc full-run recolor (the measured ~90-103 ms flip worst is the accepted stutter). The audit waveform change that made the neon audit CROSS the overboost threshold was KEPT (it now stale-pixel-validates the full-run recolor path that the old peak-at-+5 psi waveform never exercised; 0 severe / 0 stale). TEMP gradient instrumentation globals were removed with the revert. | Do not re-attempt a banded/split recolor without a concrete reason the transition will not read as a two-tone sweep; the user has already rejected that trade. The known-good state is: gradient arc + A8 segment tiles + teScanline, flip-worst ~90-103 ms. A fresh visual tear check on glass for teScanline ON remains outstanding. |
| Contract amendment (2026-08-10): organic cadence is demand-aware; constant-slew remains direct | The 2026-08-09 contract required raw median `renderFps` ≥60 on both waveforms. Quantized faces legitimately report 23-30 FPS during organic dwell windows when no visible state changed, so that criterion confused absent demand with missed work. | Firmware now publishes `gaugeDemandPerSecond`: one count for each 16 ms gauge update that introduced at least one LVGL dirty area, coalescing multiple invalidations without creating any. `tools/bench_theme_matrix.py` gates organic runs on median `min(1, renderFps / demand)` ≥95% in demanded windows and reports all-idle runs as `NO DEMAND`; legacy firmware retains the old median-60 fallback. | The constant-slew sweep remains an unchanged direct median `renderFps` ≥60 capacity gate. Historic organic measurements remain valid observations, but statements that raw organic median below 60 is itself a contract failure are superseded. Always name the waveform and demand basis when interpreting `renderFps`; never reduce demand by throttling the 16 ms gauge path. |
| Neon Segments symmetric-difference invalidation and A8 band sprites improve the tail; combined scanline candidate reaches the sweep target but organic remains below 60 (2026-08-10) | Segments repainted too much in two independent ways. Same-side/same-colour movement invalidated the angular endpoint delta, which also reflushed the whole segment containing the old endpoint even when that segment's painted state did not change. Every dirty live segment then paid three `lv_draw_arc` rasterizations (halo/body/cap), and a zone recolour could re-rasterize roughly 18 x 3 bands in one cycle. Full-size Tube/Segments readout invalidation also retained the conservative label-box Y range despite immutable glyph sprites having exact ink bounds. | Added `boost_neon_seg_diff()`, derived from the same `boost_neon_lit_span()` contract as the draw, excluding the baked zero marker and returning the minimal inclusive segment ranges whose painted state changed. Baked all 54 x 3 lit bands once through the same `lv_draw_arc` geometry into 162 colour-independent A8 coverage tiles (149,840 B PSRAM), then recolour/blit them at runtime; persistent descriptors and pixel storage outlive asynchronous draw tasks, teardown drains draw units first, and allocation failure keeps the original live-arc renderer. Tube/Segments glyph Y invalidation now uses actual immutable-sprite bounds with a clamped 1 px AA margin; fallback and marquee retain conservative boxes. | Host: geom/unit and web parity green; 25 s Tube, Segments, Marquee, and Marquee-spin audits each **517 comparisons / 0 stale / 0 severe**. Segments used zero live ring arcs, cache build ~20 ms, and host p50/p90/max recorded 0.48/1.06/4.61 -> 0.40/0.77/2.37 ms at the same 17,644 px/cycle. Controlled firmware: 2,231,184 B, SHA-256 `D0F153F70204A641B78F6E08D9D27F8AEC448B50A09B099D1E77EA6E79810ADD`; serial proved `neon segment band sprites: 162 tiles, 149840 B`, with no alloc failure, panic, stack overflow, send-colour error, or TE timeout. A8 alone, two fresh-boot 30 s constant-slew runs: **55/59 and 53/59 min/median** versus the comparable committed pre-change 28/44. Same-firmware matched scanline A/B: OFF **55/59**, ON **56/61 and 55/61**; median worst cycle 34.6 ms OFF versus 23.9/24.4 ms ON, zero TE timeouts. This meets the constant-slew median target only as the combined candidate. Organic remains below contract: 90 s ON run slow quartile **23/26**, fast quartile **29/53**; full 30 s-per-arm matrix Segments **23/30**, Dyno reference **60/62 PASS**. Do not claim completion: fresh physical-glass tear/appearance inspection is required before accepting scanline ON, and organic median >=60 remains unmet. The intermediate XOR-only arm is not reproducible and must not be used as the release baseline. |
| TE-scanline writeback implemented; Vault PEAK object shrunk | The wide-band stutter (neon segments boost→overboost flip recolours ~18 segments x 3 arc bands, measured **29/45 min/median FPS** under the constant-slew sweep) is a "second-frame tax": when the scan is inside the dirty band, `te_wait_for_region_spans()`'s fallback waits for the next V-blank - up to a full ~16.75 ms period no matter how tall the band is. The CO5300 `set_tear_scanline` (0x44) lever was stubbed (`BOOST_LCD_TE_SCANLINE`) but never actually used. Separately the Vault PEAK tell-tale's LVGL object was 34x34 although its triangle ink needs at most ~24x24, flushing ~1156 px whenever the marker moves. | Waiting for V-blank is the only safe fallback when the scan is mid-band, but its cost is the whole remaining frame, not the band's height; the scan-position estimate also assumed every edge fired at row 0, which a scanline would break. | **Dynamic scanline writeback** (runtime toggle `teScanline`, persisted, default OFF like teSync/regionDBuf; applied at boot via `main.c` and via `PUT /api/v1/themes/config`): when EARLY/LATE cannot be proved, program 0x44 to just past the region's bottom edge (`y1` exclusive + `BOOST_LCD_TE_ROW_MARGIN`) so the next TE edge arrives as soon as the scan clears the band (~0.7-4.5 ms for a ~103-row band), then burst LATE-safe (same one-frame-old-band semantics as the existing LATE skip). The scan-position estimate anchors at the programmed line (`s_te_scanline_row`, -1 = V-blank), and both fresh-window shortcuts are guarded on that anchor. Only an edge after the applicable cutoff starts the burst: unchanged targets preserve an edge racing the wait; changed targets require an edge after successful 0x44 completion so an old-line edge cannot establish the new anchor. New `teScanlineWaits` metric on `/api/v1/state`; `tools/bench_fast_motion.py sweep` gained `--te-scanline true/false`; `crossings` captures both threshold directions; `tools/mock_server.py` round-trips the field. **Vault PEAK object**: 34x34 -> `VAULT_PEAK_MARK_SIZE` 26 with the ink-extent math documented; `LV_ALIGN_CENTER` keeps mx/my fixed so the rendered pixels are byte-identical. | Host: vault audit **0 severe / 0 stale** (414 compares, 4 px = the documented isolated 1-step AA seams; flushed px/cycle mean 9879 vs 9862 pre-shrink baseline), neon geom green, sim + ESP-IDF builds clean. First hardware crossing A/B (2 events/direction; one-second telemetry windows, ~0.3-0.7 s attribution latency) showed the path active but **not a locked-60 fix**: Segments OFF 27 vs ON 34.5 median FPS (`teScanlineWaits` 0→12.5/s), Tube 47 vs 48.5 (0→19/s), zero TE timeouts throughout. Larger interleaved runs and the dyno-cell guard remain required for release evidence. A fresh **visual tear check on glass** is also still required; do not leave the toggle ON until confirmed by eye. Since both layouts remain below 60, next profile full-ring recolour and dirty-region work without visual compromise. |
| Neon: "reset to default colors" set the colours to Violet but left the preset selector where it was, so the two disagreed until the selector was cycled away and back; separately, selecting any non-Violet preset immediately reported the theme as `customized` without the user editing anything | Both `boost_theme_reset_colors()` and `boost_theme_is_customized()` compared/restored against `s_defaults[]`, which holds Violet because one palette has to be the compiled-in initial one. Neither knew about `neonPreset`. The false `customized` flag is what offers the reset, so the two bugs fed each other. | For `neon`, both now treat the SELECTED preset as the baseline: reset re-applies `apply_neon_preset()`, and `is_customized` compares against `s_neon_palettes[preset]`. | `tools/test_neon_geom.c` covers the full cycle (select a preset -> not customized; edit -> customized; reset -> back to THAT preset's colours with the preset unmoved). Verified end-to-end over HTTP for two presets. Any theme that gains a "variant" setting needs the same treatment - a table default is not the same thing as the user's current baseline. |
| The dashboard's colour picker closed by itself a second or two into typing a value or holding a slider | `renderThemes()` rebuilds the theme list with `replaceChildren()`, destroying every `<input type="color">` in it - and the picker's own edits schedule that rebuild: `input` fires, `queueThemeConfig` debounces 250 ms, the PUT resolves, and the response handler calls `renderThemes()`. Destroying a focused colour input closes its native dialog. | Defer the rebuild while a colour input is focused (`trackColorEditing`), and run the pending one when it closes. The gauge canvas still repaints from the `input` handler, so the live preview is unaffected. | Any poll- or response-driven `replaceChildren()` over a container holding focused, stateful controls (colour inputs, open selects, text fields mid-edit) will fight the user. Guard the rebuild, do not shorten the debounce. |
| Simulator screenshots were validating the wrong colour table, silently, for an entire session | `boost_theme_find()` reads `s_themes` directly and `ensure_loaded()` only memcpys `s_defaults` into it; `apply_neon_preset()` runs from `boost_theme_init()`, which `sim/main.c` never called. So every sim render showed the compiled-in palette regardless of `neonPreset`, and a preset's colours could not be checked there at all. It stayed invisible because `s_defaults`' neon entry and the Violet preset were being edited together - the moment one changed without the other, a screenshot disagreed with the device. | `sim/main.c` calls `boost_theme_init()` before option parsing and gained `--neon-preset 0..3`; `s_defaults`' neon entry is realigned with the Violet preset and carries a comment that it must mirror it. | The sim is the primary verification loop, so it has to run the same init the firmware does. When a host harness and the device disagree, suspect the harness's initialisation before the rendering code - and be suspicious of any "default" duplicated in two tables that are edited by hand. |
| Neon readout: minus sign glow read as a solid slab, decimal halo cut off square at top and sides, and cell pitch kept having to grow to stop digits clipping each other | One cause: the baked glyph sprites were OPAQUE RGB565, so every tile painted its own black margin, and at the readout's pitch neighbouring tiles always overlap. The later blit erased the earlier glyph's glow and, for the widest digit pairs, columns of its ink. Each previous fix pulled the same lever - widen the cells until the margins stop colliding - which is what made the readout too wide and forced the decimal's glow margin to zero (cropping, not removing, the blurred halo). | Store one 8-bit A8 coverage plane per glyph and blit with `recolor`; LVGL blends an untransformed A8 image as solid-colour-through-mask, so zero coverage contributes nothing and overlapping glows composite. Decimal regains a full margin; cells pack to ink, not to margins; coverage is colour-independent so the zone dimension disappears (874 KB -> 149 KB at 118 px) and ink becomes a per-blit recolor tracking the live accent. The minus mark moves into the same pipeline (`neon_bake_sign_sprite`) - it was two sheared bars whose "glow" was the same bars inflated 5 px, which at a 6 px bar height on an 8 px pitch overlapped them and flooded the 2 px gap. | Never reintroduce opaque tiles for overlapping art. `neon_sign_x_span()` must bound the sign by its BAKED footprint, not by width-plus-pad: raising the pad to the blur's reach pushed the readout's dirty rect into the ring band, costing flushed pixels every readout update and leaving an AA seam where the arc got clipped (audit caught it as 1 severe px at r=199.6). |
| Neon tube: the white zero marker looked skinny, and appeared to jump sideways when crossing from vacuum to boost | The marker is baked into the static background, but the live lit run starts exactly at `a_zero` and sweeps outward at the marker's own radius and width, so it painted over whichever half lay on the value's side. Only 2 of the baked 4 degrees ever survived, and the buried half swaps sides at the crossing. Widening the bake alone could never help. The segments layout never had this because its loop skips the zero segment and lets the bake show through; the tube run is one continuous arc with no such seam. | Redraw the marker live after the run (`NEON_TUBE_ZERO_DEG`, +-3 deg), keeping the bake so it still shows with nothing lit. Costs one arc per frame, tube only. | Any face element drawn UNDER a live sweep at the same radius and width must be repainted after it, or verified to sit outside the sweep. Measured constant 6 deg at 233-239 deg in both vacuum and boost. |
| Neon ring's two-tone gradient disappeared on the panel - three bands read as one flat colour - while the web mirror still showed distinct zones but with all three zone colours converged | Two defects sharing a root. On the panel, `neon_lit()` had been changed to clamp overflow PROPORTIONALLY (divide by 255/peak) to fix an earlier zone-convergence bug; for a saturated entry that factor is ~0.46, so the "bloomed" body came out DARKER than the raw palette drawn behind it as the halo. Measured over four palettes the band ordering was inverted in 11 of 12 zones - the gradient ran backwards, not merely low-contrast. In the mirror, `neonLitColor()` still clamped PER CHANNEL, the original convergence bug, never ported; the parity test could not catch it because its own reference implementation encoded per-channel clamping too, so mirror and test agreed with each other while both drifted from the firmware. | `NEON_HALO_DIM` 0.55 - the ring's inner band is a DIMMED zone colour, not the raw palette entry, which fixes ordering at its source and costs nothing in zone separation (worst halo/body contrast 4 -> 125). `NEON_WHITE_LIFT` 0.35 - overflow desaturates toward white instead of being clipped (hue shifts, zones converge) or scaled away (hue kept, all gain lost). Net: 0/12 inverted, worst zone separation 158. Web reference now derives from `NEON_BLOOM`/`NEON_SAT`/`NEON_WHITE_LIFT` read out of `boost_gauge.c`. | A parity test whose reference restates the behaviour under test cannot detect drift - derive it from the firmware source. `tools/test_neon_web_parity.js` now also asserts the band-ordering property directly over all ten palette colours, so a bloom change that inverts the bands fails rather than merely looking wrong. |
| Entering the neon theme took ~350 ms against 45-100 ms for every other theme | The 466x466 background canvas was repainted and all 13 glyph tiles rebaked on every scene build. The device had never reported its own build time because the only timing log was gated behind `BOOST_NEON_DRAW_STATS`, a host-sim flag - so the cost had never been split or measured on hardware. | Log unconditionally (one line per scene build, split bg vs bake: it showed bg 223 ms + bake 214 ms). Memoize both across scene switches as `s_vault_bg_buf` already did. The glyph cache key is JUST the layout, which is only possible because the tiles are A8 coverage and carry no colour - no palette edit, preset change or zone flip can invalidate them. Background keys on `neon_bg_key_t` (layout, track, zero, and where zero lands on the sweep - one `psi_to_sweep()` call rather than psiMin/psiMax/zeroAngle separately, taken from the same call the paint makes so it cannot disagree). | Verified on hardware in both directions via serial: re-entry 2 ms (both cached), preset change with a different `track` 225 ms (bg repaints, bake cached), same preset again 2 ms, layout change 439 ms (both rebuild). A cache whose invalidation is only reasoned about is not verified - exercise the paths that MUST rebuild, not only the fast path. |
| Three consecutive commits of neon web-mirror work were built and flashed with none of it live on the device; symptom presented as a firmware rendering bug (zones converged in the browser but correct on the glass) | `main/generated_web_assets.c` is regenerated by hand and is NOT part of the ESP-IDF build (AGENTS.md already required the step; it was simply not run). Compounding it, the assets are served gzipped, so grepping the raw HTTP response for a newly added token finds nothing whether the asset is current or stale - which produced a confident wrong diagnosis before the right one. | Ran `tools/embed_web.py`, reflashed, and verified by DECOMPRESSING what the device serves. | Treat "the panel and the mirror disagree" as a stale-asset hypothesis first, before a rendering one. Verify served assets by decompressing, never by grepping the raw response. See the Web assets section for the one-liner. |
| Earlier | AMOLED became half-white/half-green with `ESP_ERR_NO_MEM` and `send color data failed`. | Stock BSP placed LVGL buffers in PSRAM and the bounce-buffer path failed under partial refresh. **Corrected 2026-07-28:** this row previously read "ESP32-S3 SPI GDMA could not stream them", which overstates the mechanism. `SOC_PSRAM_DMA_CAPABLE=1` on the S3 and other GDMA peripherals on the same chip DMA from PSRAM routinely (i80 LCD, RGB LCD, UHCI, parlio, camera DVP). The real constraint is narrower: the general-purpose SPI master driver hard-codes `access_ext_mem = false` on its GDMA channel (`esp_driver_spi/src/gpspi/spi_common.c:257-263`, directly above `// TODO: add support to allow SPI transfer PSRAM buffer`), so PSRAM sources get bounce-copied per transaction. A driver configuration choice with an open upstream TODO, not a hardware incapability. | Dedicated `boost_display_start()` with internal DMA buffers, bounded strips, and the existing partial-refresh/rounder path. | Hardware 30-second soak, no serial display errors, and median physical cadence ≥60 FPS. Do not cite this row as evidence that S3 DMA-from-PSRAM is impossible. |
| Earlier | Large media writes reached approximately 172 s; a tuned SPIFFS run measured 7.37 s but remained fragile. | SPIFFS garbage collection/large-file staging was the bottleneck and was not a robust atomic media store. | Raw `media` partition with inactive-slot streaming, payload CRC, newest-valid-generation scan, and committed-header-last publication. | Complete 1,379,129-byte hardware upload in 7.504 s; verify reboot selection and preservation on abort. |
| Subsequent | Upload replacement and Delete must not race, and a canceled transfer must not destroy the old GIF. | Publication and request ordering must be serialized; an incomplete inactive payload is never a committed media slot. | Browser aborts XHR and waits for settlement before DELETE; firmware serializes operations and returns 409 for overlap; abort preserves the old slot. | Exercise cancellation, overlap rejection, and two repeated deletes on hardware. |
| Current architecture | Physical, network, browser, fallback, and sparkline rates can be mistaken for one cadence. | Different producers and renderers have different budgets. | Explicit 16 ms/~60 Hz physical path; fixed 3-client WebSocket pool with one heap-owned in-flight frame per client, 750 ms application heartbeat, and 62.5 Hz notification-driven broadcast; rAF/35 ms EMA smoothing with ordered `uptimeMs` targets; 4 Hz HTTP/sparkline paths; DPR cap 2 and GIF preview disabled. | Keep rate-specific metrics and verify physical cadence separately; the badge must identify WebSocket versus HTTP fallback. |
| Current architecture | Concurrent or stale dashboards evicted each other, causing transport churn and out-of-order target jitter. | Single-owner WebSocket server forcibly closed the old file descriptor whenever a new dashboard connected. | Fixed pool of 3 WebSocket clients; each client has one in-flight heap-owned frame, bounded 62.5 Hz broadcast, and a 750 ms browser heartbeat consumed by the server. | Three clients must remain WebSocket OPEN while a fourth uses HTTP fallback; browser rejects non-newer targets and resets smoothing timing after gaps over 1 s. |
| Current architecture | Local CSV timestamps carried a misleading timezone suffix and time behavior was unclear across reboot. | Timestamp formatting conflated local text with offset metadata; dashboard open was mistaken for synchronization. | `timestamp_local` uses `%Y-%m-%dT%H:%M:%S` without suffix; `utc_offset_minutes` stays separate. Sync Time POST supplies `Date.now()` and configured offset; firmware calls `settimeofday`, saves epoch + monotonic checkpoint/config to NVS, and restores/advances by monotonic delta when applicable. | Hardware CSV: 1,800 rows, `badTimestampCount 0`, offset `-300`; no RTC/NTP resync in this path, so only the Sync Time control synchronizes. |
| Current architecture | A fourth dashboard appeared to fail completely or churned `Disconnected` while HTTP fallback was healthy. | The three-client pool rejects only the newcomer, but browser fallback lost its 250 ms constant and reconnect attempts overwrote healthy fallback status. | Restore `POLL_FRAME_MS = 250`; keep `Live` after successful state polls; do not downgrade status merely because a retry starts. | Hold clients 1–3 open, open client 4, verify clients 1–3 remain open while client 4 polls at 4 Hz and retries every 1 s; only total transport failure may show `Disconnected`. |
| Current architecture | GIF playback ownership was unclear and could be described as a project-written decoder. | The pipeline mixes custom storage/UI/display integration with locally patched third-party LVGL and AnimatedGIF decode layers. | Document the boundary: custom dual-slot mmap and widget lifecycle; LVGL timer/wrapper; AnimatedGIF parsing/LZW/composition; zeroed full canvas and standard non-turbo path for delta/disposal correctness. | Verify `GIF_openRAM`/`GIF_playFrame`, keep mapping alive through widget destruction, test delta/disposal GIFs, and keep the internal-DMA CO5300 flush separate from decode changes. |
| Current architecture | Reflashing while a committed GIF existed caused a repeatable `main` task stack overflow before networking started. | Boot-time payload validation put a 4,096-byte CRC scratch buffer on the 3,584-byte ESP-IDF main-task stack. | Validate the committed payload through a temporary read-only `esp_partition_mmap` and CRC it directly, then unmap; no full sector lives on the stack. | Reboot with committed media present and require `HTTP API ready` with no stack overflow, panic, or reset loop. |
| Current architecture | Refreshing the cockpit could replace a healthy WebSocket badge with HTTP, while in-page settings hid cockpit controls and range changes moved the zero notch. | Shared browser bootstrap treated all controls as co-located, refresh always labeled its HTTP samples as active transport, and dial geometry used one linear range. | Cockpit and `/settings.html` share guarded bootstrap; refresh preserves an open WebSocket label; the gear navigates to settings; a fixed 236.25° zero has independently scaled vacuum and boost sides. | Host mock: cockpit and settings boot with no visible error, settings saves −12 / 15 / 11, cockpit reports `Live · HTTP 4 Hz` without WebSocket; review the mapping so `psiToAngle(0) = 236.25°` for every valid range. |
| Current architecture | Raising max PSI to 15 produced the automatic 7.5 PSI midpoint beside an 8 PSI overboost marker. | The fixed five-label set always emitted `psiMax / 2` and `psiOverboost` without checking rendered separation. | `zeroAngle` is persisted/configurable from 180–315° (default 236.25°); min, zero, overboost, and max remain mandatory, while the boost-half label is hidden when its projected label center is too close to overboost. Legacy NVS range blobs are migrated to retain user settings. | Set max 15 / overboost 8: only the 8 overboost label remains on both hardware and browser. Change zero angle and reboot: API and notch retain the selected value. |
| Theme system (multi-style faces) | A "theme" was only a palette swap over one arc face. Wanted four visually distinct, selectable, persistent gauge designs. | Rendering was hard-coded to a single arc layout; the theme carried colours only. | `boost_theme_t` gained a `style` (`arc`/`vault`/`hud`/`bigdigit`), emitted in `/themes`. `boost_gauge.c` became a scene dispatcher: `build_scene`/`destroy_scene` per style, `boost_gauge_update` dispatching to `update_*`. Theme change **rebuilds** the scene rather than recolouring. `psi_to_sweep()` projects the arc zero-referenced scaling into each face own sweep so every style honours `zeroAngle`. Web mirrors all four in `web/app.js`; `sim/` renders them headless. | Keep the `arc` face geometry and wedge invalidation byte-for-byte: it is the path the 60 FPS guard was set against. After any style change re-run the cadence guard **per style** and compare `pixelsPerSecond`, not just `renderFps`. |
| Theme picker did not reach the panel | Selecting a theme in the dashboard only took effect after a reboot. | `PUT /themes/active` persisted the id but never called `boost_gauge_apply_theme()`; only the `/config` path did. | `theme_active_put` applies the theme under `boost_display_lock`. | Switch themes from the dashboard: the panel must change immediately and survive a power cycle. |
| Stale-pixel smearing on moving elements | Trails/artifacts behind the needle and HUD fill, clearing only seconds later. | Elements were **drawn from the live sample** but **invalidated on a threshold**, so skipped frames repainted at a new position without clearing the vacated region. The needle repainted only on angle change, so an overboost colour flip at a static angle left the old colour on screen. | Every moving element draws from a committed value advanced only when it invalidates (`s_vault_needle_deg`/`s_vault_needle_over`, `s_hud_fill_deg`/`s_hud_fill_psi`). Needle invalidation padding must cover the hub **plus its border**. | Sweep through zero and through overboost both directions: no trails, no leftover colour at the hub. |
| Full-face repaints are inherently a hitch | The `bigdigit` colour sweep visibly stutters at each step. | Recolouring the whole panel is ~217k px. Measured worst render cycle for one unbanded recolour: **45 ms**. Nothing tunable removes the work. | Quantize the sweep (`BIG_STEPS`), never recolour the ground per frame, and **spread** the repaint that remains: `BIG_BANDS` full-width bands take the new colour on successive ticks. Measured worst cycle: 1 band 45.1 ms (no wipe), 2 bands 33.3 ms (selected), 4 bands 25.8 ms (visible top-to-bottom sweep). Caching does not apply here — a flat fill is already the cheapest primitive, and 24 cached grounds would want 10 MB. | A/B any change to `BIG_BANDS` on `worstRenderUs`, not `renderFps`: banding conserves total work and so barely moves FPS. The QSPI clock is not the lever — 14.5 ms of that 45 ms is the link at 60 MHz, so 80 MHz buys 8% and 40 MHz costs 16%. |
| Static face art re-rasterised every frame | Rich faces (vignette, scanlines, dense ticks) were unaffordable; the needle felt clunky and Vault fell to 37 FPS (min 4). | Every dirty region re-ran the full vector program for the static art. The ESP32-S3 has no 2D accelerator, and the bottleneck is CPU rasterisation, not the QSPI link (measured: 6% bus utilisation at 60 MHz; GIF blits sustain 3.16 Mpx/s vs the gauge's 0.9). | Paint the static face **once** into a PSRAM `lv_canvas` at scene build and blit thereafter (`paint_vault_background`). Moving parts stay separate objects above it. Vault went to median 61 / min 54 **with** vignette and scanlines added. | Keep the completed Vault canvas across theme switches as specified by the newer cache-key ledger row; other face caches are freed in `destroy_scene()`. Never put moving elements in the cache. Raising the QSPI clock is not a fix — the bus is ~6% utilised. |
| `render_fps` misread as a smoothness metric | Static-content faces reported 28-40 FPS and looked like a regression. | It counts `LV_EVENT_RENDER_READY`, i.e. render cycles actually performed, and LVGL only renders when something is invalidated. Fewer content changes means fewer renders, i.e. *less* work. | Compare `pixelsPerSecond`: `vault` sustains ~0.9 Mpx/s at 60 FPS while `bigdigit` uses ~0.48 Mpx/s at ~30. | Do not optimise a face whose throughput is well under the demonstrated ceiling; confirm perceived stutter visually first. |
| No metric for perceived choppiness | `renderFps` and `pixelsPerSecond` both looked healthy on faces that visibly lurched. | Neither captures **latency**. A face can average well and still block the pipeline for 45 ms once a second. The first attempt measured the *gap* between `RENDER_READY` events, which is worthless: an idle screen shows huge gaps and no stall. | `worstRenderUs` times `LV_EVENT_RENDER_START` → `LV_EVENT_RENDER_READY`, the duration of the cycle itself, reset per one-second window and exposed on `/state`. | Measured worst cycle: `arc` 43 ms, `vault` 18 ms, `hud` 37 ms, `bigdigit` 26 ms. `arc` is now the worst offender and still has an uncached ground. |
| OTA could not be completed remotely | `POST /ota` wrote and validated the image, set the boot partition, and returned `restartRequired: true` - with no endpoint able to restart. The only way to finish an update was to power-cycle the panel by hand, which for a car install means pulling the dash. | The feature was built as upload-only; nothing ever exercised the whole path. | `POST /api/v1/restart` fires `esp_restart()` from a 400 ms one-shot timer so the HTTP response is delivered first. `uploadOta()` now calls it and treats a dropped connection as success, because a device that reboots on cue cannot answer. | Verify OTA by checking the boot log for `Loaded app from partition at offset 0x420000` (ota_1). A device that still boots 0x20000 is running the serial-flashed image and proves nothing. |
| NVS-backed settings saved but vanished on reboot | Theme colour overrides applied instantly, persisted, read back correctly - and were gone after a restart. | `boost_theme_init()` ran before `boost_model_init()`, which is where `nvs_flash_init()` lived. `nvs_open()` on an unmounted NVS just fails, so the boot-time read was skipped silently; later writes worked because NVS was up by then. Failure is invisible until a power cycle. | `boost_theme_init()` mounts NVS itself (idempotent, with the same erase-on-corrupt handling), so it no longer depends on call order. | Never let a module's persistence depend on another module having initialised NVS. Test persistence with an actual reboot - a set/get round trip in one boot cannot catch this. |
| Allocator tuning aimed at the wrong allocator | `heap_caps_malloc_extmem_enable(64 KB)` around GIF widget creation was expected to move the decoder's ~24 KB of LZW tables into internal RAM. Internal free moved by 112 B: nothing happened. | `CONFIG_LV_USE_CLIB_MALLOC=y` is set, but `lv_conf_internal.h` reads **`CONFIG_LV_USE_STDLIB_MALLOC`**, which is never defined - so `LV_USE_STDLIB_MALLOC` falls back to `LV_STDLIB_BUILTIN` and LVGL allocates from its own pool, never `malloc`. The extmem threshold only affects `malloc`. | Allocate the `GIFIMAGE` separately inside the widget rather than embedding it in `lv_gif_t`, so its placement is under our control. | A Kconfig symbol existing is not evidence it is the one being read. Confirm placement empirically (`esp_ptr_external_ram()` plus free-heap deltas) before crediting an allocator change. |
| Internal RAM taken for speed starved the radio | Moving the 24.5 KB GIF decoder state into internal RAM boot-looped the device: `Guru Meditation LoadProhibited` with a backtrace through `wifi_softap_start` / `ieee80211_hostap_attach`. | The GIF loads at ~3 s, before SoftAP attach. Taking 24.5 KB left ~49 KB internal, and the Wi-Fi driver faulted on a failed allocation rather than failing gracefully. Because the crash precedes the HTTP server, OTA cannot recover it - the fix must go over serial. | Reverted to the pooled allocation. The `GIFIMAGE` pointer indirection was kept, so a future attempt can place only the hottest few KB. | Internal DRAM is shared with Wi-Fi and display DMA. Measure free internal RAM at *peak* Wi-Fi usage, not at the moment of allocation, and keep a hard reserve. Anything that can brick the boot path needs a serial recovery plan before it is flashed. |
| Needle judder blamed on the wrong thing, twice | The needle looks uneven - "slower, then speeds up to catch up". First suspect was the 0.35 deg invalidation gate; second was LVGL's swdraw threads sitting at priority 3, below the sample task and round-robin with the telemetry task. | Both falsified. The gate is a deadband accumulator, so position lag is bounded by the threshold itself: max 0.35 deg = **1.03 px** at the tip - and the arc face has no angular gate at all yet shows the same symptom. Raising `CONFIG_LV_DRAW_THREAD_PRIO` 3 -> 4 and dropping `boost_ws` to 2 was flashed and A/B'd: frames-over-budget per second went 3 -> 4, i.e. no change. | Added `renderGapP50Us` / `renderGapMaxUs` / `framesOverBudget` (START-to-START) to `/api/v1/state`. They show median gap a clean 16.0 ms with **2-4 gaps per second over 20 ms**, present even with zero clients connected. The cause is renders occasionally exceeding the 16 ms refresh period, so a cycle is skipped and the gap doubles to 32 ms. | Judder is a *pacing* problem and needs a *pacing* metric; `renderFps` and `worstRenderUs` both looked fine throughout. Reduce peak per-render cost on the vault face before touching schedulers again. |
| Tearing on the needle, and why TE is not a quick fix | Occasional horizontal break across the needle. | The panel *is* driving its tearing-effect line - the vendored init sends `0x35 0x00` (`boost_display.c`) - but the ESP never listens: `tear_avoid_mode = NONE`, `te_sync = TE_SYNC_DISABLED()`. The needle's dirty band is 205 of 466 rows, so the scan crosses it in ~7.3 ms while a render takes ~12.7 ms wall clock; unsynchronised, a partial write lands mid-scan essentially every frame. | Not fixed. The BSP defines no TE GPIO and nothing in the tree names one - **whether the pad is even routed needs the Waveshare schematic**. And the adapter's stock TE path waits per *flush*, which at 20-line strips means 2-3 waits per needle update (20-30 Hz) and 400 ms for a full-screen repaint: strictly worse than tearing. | If TE is ever wired, gate once per render cycle via the adapter's `custom_draw_bitmap` hook (honoured even in `TEAR_AVOID_MODE_NONE`), not per flush. Note the 0.35 deg gate is a tearing *amplifier*: fewer, larger steps make each tear more visible. |
| A settle gate that never opened | Pixel shift was meant to wait for a steady reading so its full-screen repaint lands where nothing is moving. | `PXSHIFT_SETTLE_PSI` was 0.25 psi over 1.5 s, but the signal's own flutter is `0.18 sin(17.3t) + 0.08 sin(31.1t)` - up to 0.52 psi peak-to-peak. Longest window inside +/-0.25 psi is 0.69 s, so the gate passed **0.0000%** of the time and every shift fell through to the 6-minute deadline, mid-sweep: precisely the case it existed to avoid. | Raised to 0.60 psi, above the noise floor. | Any "wait until quiet" test must be checked against the noise amplitude of the signal it watches, or it silently degrades to its own timeout. A real MAP sensor will be noisier than the simulator. |
| TE **is** routed - GPIO13 | Tearing was written off as possibly unfixable because the BSP names no tearing-effect pin and nothing in the repo referenced one. | The BSP simply does not expose it. Decoding the netlist out of the board schematic: `PIU1017 NLGPIO12 NLLCD0CS` and `PIU1018 NLGPIO13 NLLCD0TE`. The GPIO12/LCD_CS pair matches `BSP_LCD_CS` exactly, which validates the decode - so **LCD_TE is on GPIO13**, carried on the J3 display FPC, and GPIO13 carries no other net. | Not yet implemented. | Do not conclude a signal is unrouted from the BSP alone; a vendor BSP exposes what its own demo used, not what the board has. |
| Dropped frames are per-face, and the arc face proves the pacing is achievable | Judder assumed to be a global property of the display stack. | Measured with the new gap metric, zero telemetry clients, 14 s per face: **dyno-cell 0 gaps/s over 20 ms**, vault-tec 3, night-city 10, big-digit 14. The arc face holds a 16.0 ms median with a 16.6 ms maximum - textbook pacing. Drops scale with per-render cost, not with anything global. | Reduce peak per-render cost on the offending faces. | `worstRenderUs` right after a theme switch reports the scene *build* (50 ms), not steady state - discard the first samples after a PUT. |
| Alpha-composited logo art seamed at overlaps | The Vault-Tec mark's ring, hub and bars were each stroked at LV_OPA_50; wherever two overlapped the alpha composited twice and left a visibly darker seam. | Per-shape transparency composites per shape, not per figure. | Pre-blend the colour once (`lerp_rgb(face, text, 0.5)`) and draw every element at LV_OPA_COVER. One flat tone regardless of overlap, and cheaper. | Any multi-part figure that wants uniform translucency must be pre-blended, drawn into its own layer, or built from non-overlapping pieces. |
| Linear vignette ramp left visible rings even when dithered | Faint rings survived the 8x8 Bayer dither, plus a dark-light-dark band near the rim. | A linear ramp has a slope corner where it starts and where it clamps. On a face whose green sits at level 4 of 63 those corners are among the few real transitions there are, so dithering could not hide them - the panel's own radial profile showed a step at exactly r=120, the ramp start. | Smoothstep (`t*t*(3-2t)`), which has zero slope at both ends so the ramp fades in and out. | On a near-black face, curve shape matters more than dither quality. Measure the radial profile off a panel snapshot rather than judging by eye. |
| TE sync engages but does not stop the needle tearing | With teSync on, `teWaits` runs at ~57/s and `teTimeouts` at 0 - the gate is genuinely waiting on the vblank edge every render cycle - yet the tearing is unchanged. | Not polarity and not a broken toggle. The pipeline renders partial: each 20-line strip is rasterised (~4 ms) then flushed, interleaved. Waiting for vblank only syncs the FIRST strip; the scan advances ~35.8 us/line while rasterisation of later strips takes milliseconds, so the scan outruns the writes and later strips still land mid-scan. TE-at-vblank fixes the phase of the start, not of every strip. | None that is cheap. A real fix needs the whole dirty region rendered to an off-screen buffer first, then DMA'd back-to-back inside one vblank - i.e. region double-buffering, a larger change. Left as a runtime toggle (off by default) since it is harmless and marginally changes pacing. | TE sync only eliminates tearing when the transfer, not the rasterisation, is the thing racing the scan. On a CPU-rasterised partial pipeline it cannot. Confirm with `teWaits`/`teTimeouts` before assuming the control is broken. |
| Toggle unchecks itself but the effect sticks | The arc/hud gradient checkbox cleared ~250 ms after being ticked, though the gauge kept the gradient. | `queueThemeConfig`'s response handler resynced only the two big-digit booleans from the payload, then re-rendered the editor, which read the still-false `state.arcGradient`. The device had saved it; the UI mirror had not. | Resync every toggle field the endpoint can return, not a hand-picked subset. | A debounced write that re-renders from local state must fold the whole response back into that state first, or the render races the save. |
| Documented a setting that was never in effect | The repo described an "active 60 MHz QSPI trial"; performance analysis was built on that number for months. | There was no override anywhere. The clock lives in `CO5300_PANEL_IO_QSPI_CONFIG` inside a managed component, `bsp_display_new()` takes its 40 MHz default with no hook, and edits under `managed_components/` are reverted by any dependency refresh. Every measurement predating this was at 40 MHz. | Vendor the bring-up: `panel_new()` in `boost_display.c` is `bsp_display_new()` plus the 14-entry init table, with `pclk_hz = BOOST_LCD_PCLK_HZ` (80 MHz). Boot logs `panel up at N MHz QSPI` so the active value is observable, not asserted. | Never configure hardware from `managed_components/`. If a doc claims a hardware setting, there must be a line of our code to point at and a boot log line to confirm it. |
| Vendoring bring-up silently broke brightness | `bsp_display_brightness_set(90) failed: ESP_ERR_INVALID_STATE` on every boot after `panel_new()` replaced `bsp_display_new()`. | The BSP setter writes through a **file-static** `panel_handle` assigned only inside `bsp_display_new()`. Taking over panel creation leaves that static NULL forever. | `boost_display_set_brightness()` issues the same single 0x51 command through our own `s_panel_io`. | When replacing a BSP entry point, grep the BSP for file-statics it assigned; anything else in that component reading them is now broken and will only show up in the boot log. |
| QSPI clock treated as a general performance lever | Expected a faster link to lift every face. | Only full-frame pushes are link-bound. Measured 40 -> 80 MHz: `bigdigit` full-screen recolour 45.1 -> 37.6 ms (-17%), but `arc` 43.2 -> 41.7 and `hud` 36.8 -> 37.3, i.e. nothing. Partial-update faces are CPU-rasterisation-bound with dirty regions far too small for the link to matter. | Keep 80 MHz for the one operation it helps; do not cite it as a fix for partial-update cadence. | Before reaching for the clock, check whether the operation in question actually pushes a full frame. |
| Smooth gradient banded into rings on the panel | The Vault vignette showed four hard rings even after the arc approximation was replaced by an exact per-pixel radial ramp. | Not the gradient maths — the **destination format**. The face is `#02100a`, whose green is level **4 of 63** in RGB565, so darkening toward black has only four values to land on. The web mirror is smooth only because canvas is 8-bit (green 16 -> 6). | Ordered dithering: an serpentine Floyd-Steinberg-style error diffusion added before truncation in `paint_vault_background`, trading spatial noise for tonal resolution. Baked into the cache, so it never shimmers. | Before treating a banding report as a maths bug, compute the source channel's 565 level. Any smooth ramp over a near-black colour needs dithering on this panel. |
| Vignette/scanlines diverged from the web mirror | The physical Vault face looked wrong next to `drawVaultGauge`, and earlier scanline attempts tore per-region. | The vignette was nine concentric arcs (visible banding, and it started at r=165 instead of the web's r=120); the scanlines were baked *under* the art and ran edge-to-edge over the square instead of being chord-clipped to the face. | Port the web's method, not just its numbers. The vignette is a **per-pixel pass over the canvas buffer** (`lv_canvas_get_draw_buf`, RGB565 via `lv_color16_t`) after the vectors are drawn — free once, and exact. The scanlines are **not** baked: `draw_vault_crt` is the topmost child so they cross the needle and digits, with phase from **absolute screen y** so regions cannot disagree, chord-clipped per row and re-clipped to the dirty area. Cost ~1 FPS. | Any overlay that must sit above moving art has to derive its pattern from absolute screen coordinates, never from the region being drawn. |
| Widget-sized invalidation on a small change | Night City sat at 32 FPS median with the permanent glitch. | The ghost pass invalidated a fixed 310x100 box on every value change, though usually only the tenths digit moved. | Track which slots changed and bound the dirty box to those, grown by `HUD_GLITCH_DX + 1`. Sign resolution moved above the invalidation so a sign flip can widen the box. With the cached face this took the style to 42 median / 31 min. | Share the offset constant between the draw and the invalidation; a hard-coded literal in one of them silently leaves ghost pixels behind. |
| Wi-Fi dropped after a network scan | Scanning from the dashboard knocked the gauge off the LAN for seconds. | `boost_network_scan()` unconditionally re-applied the STA config afterwards; `esp_wifi_set_config()` on a live association forces a disconnect, and the follow-up reconnect was skipped because `s_sta_got_ip` was still true (the disconnect event had not fired yet). | Only change mode / re-apply config when the radio was not already in a scan-capable mode. | Scan while joined to the LAN and confirm the association is never dropped. |
| Escaped UTF-8 mangled through tooling | Arrow/hazard glyphs rendered as one missing-glyph box per byte; a NUL ended up embedded in the source. | Escape sequences were interpreted a layer early, so the source held the literal a-circumflex bytes instead of the intended codepoint. LVGL and the font were both correct. | Draw such marks as **shapes**, not glyphs. When scripting edits use raw strings and assert every replacement. | Grep for non-ASCII bytes outside comments and for NUL bytes before building. |
| Empty sensor bus reports changing phantom addresses | With both sensors and the level shifter removed, an RX/TX bus carrying only 4.7 kOhm pull-ups returned different one-off addresses on every full sweep. | The manual control at unused `0x55` measured 0/64 ACKs with both idle-low counters zero, repeated driver probes at the same address returned 64/64 `ESP_ERR_NOT_FOUND`, and the onboard CST9217 control returned 8/8 ACKs. This strongly implicates the changing-address `i2c_master_probe()` sequence but does not rule out every GPIO/peripheral or data-pattern-dependent electrical mechanism without waveform capture or an alternate implementation. | Keep device transactions at 100 kHz, expose the three boot controls in `/api/v1/sensors/scan`, preserve bus/device handles during recovery with `i2c_master_bus_reset()`, serialize reset against HTTP scans, and require four consecutive probe ACKs before reporting an address. | On hardware, 20/20 empty-bus scans must return no devices while boot controls remain manual 0/64 with idle-low counters zero, fixed-driver 0/64 successes, and onboard 8/8. This validates the scanner only; reconnect one sensor at a time and require sustained valid reads to diagnose the original lockup. |
| Direct BMP control still has no reliable ACK | A GY-BME/P280 module wired directly at 3.3 V on GPIO17/18 remained absent even though the original ADS/MAP installation had once produced real pressure-responsive readings before failing. | Six live fixed-address tests gave `0x76` only 0-4 successes per 32 probes, statistically indistinguishable from the absent `0x48` control at 0-2/32; every four-ACK sweep stayed empty and `bmpPresent` stayed false. Voltage-high readings alone do not prove continuity because both the MCU side and module have pull-ups. | `/api/v1/sensors/scan` exposes 32-attempt `fixed48`/`fixed76` counters serialized against bus reset. Treat 32/32 as address presence only; identity and sustained reads remain separate gates. | Before another firmware theory, power off and continuity-test endpoint-to-endpoint VCC, GND, SCL, and SDA; require near-zero ohms on intended conductors and no SCL/SDA short to ground. |
| Sensor bus restored; the diagnostic scaffolding was retired | Both devices came back on the **original** GPIO18/17 pins at 100 kHz: `found:["0x48","0x76"]`, `fixed48`/`fixed76` 32/32, `fault:false`. | The bus was restored by replacing the ADS1115 assembly. GPIO17/18 were never damaged, the RX/TX remap (GPIO43/44) was never needed, and the 400 kHz to 100 kHz reduction was **not** the fix. 100 kHz is retained on its own merits: a MOSFET level shifter with 4.7 kOhm pull-ups does not comfortably support 400 kHz in a vehicle. | Deleted the diagnosis-only paths: the manual `0x55` GPIO bit-bang probe, the fixed `0x48`/`0x76` counters, the onboard `0x5A` control and its BSP dependency, and the `bootDiagnostic` JSON. `/sensors/scan` is back to `{busUp,recoveries,found}`. Kept the durable fixes: in-place `i2c_master_bus_reset()`, the bus-admin mutex, four-ACK filtering, and the recoveries counter. | Do not reintroduce per-request fixed probes: the removed version fired 64 fixed probes plus a 448-probe sweep while holding the bus-admin lock. Keep the bench history in `docs/troubleshooting/i2c-sensor-bus.md`, not the README. |
| A wrong transfer function read +9.6 PSI of boost on a bench | The gauge showed `psi 9.63` sitting at atmosphere with both sensors healthy: `mapVolts 1.5735` converted to `mapAbsKpa 165.00` against `ambientKpa 98.58`. | The placeholder GM 3-bar approximation `(V - 0.6) / 0.0059` was never validated against the datasheet. It is not close: it overstates a real atmospheric reading by ~69 kPa. The sensors being *present and reading correctly* is what made it dangerous — every diagnostic looked healthy. | Two-point GM 12223861 fit at the curve's defined 5.00 V supply (0.619 V → 40 kPa, 4.818 V → 304 kPa), with ratiometric normalization for the actual supply, plus a manual one-point atmospheric offset against the BMP280. `boost_sensors_nominal_kpa()` is the only site the arithmetic lives. | `tools/test_map_conversion.py` asserts both datasheet endpoints, ratiometric normalization, and that recalibration replaces rather than accumulates. A sensor path that reads plausibly is not a sensor path that reads *correctly* — check a known pressure, and atmosphere is the one always available. |
| Presence flags outlived the sensors they described | `adsPresent` and `bmpPresent` both reported `true` throughout a total bus outage. | They record what answered once during `boost_sensors_init()`. Nothing downgraded them when every subsequent read failed, so any consumer gating on them was reading a boot-time fact as a live one. | `boost_sample_t` carries explicit freshness: `ads_age_ms`, `bmp_age_ms`, `bmp_updates`, and `ambient_is_fallback`. Timestamps are stamped **only on a successful read**, so age grows monotonically while a sensor is silent. Calibration and the Night City ATM readout gate on freshness, never on the presence flags. | Never let a boot-time probe result stand in for liveness. `UINT32_MAX` means never-read and must render as unavailable, not as a number. |
| The cadence guard cannot pass on real sensor data | `tools/check_display_cadence.py` demands a median of at least 60 FPS; a healthy board on the real sensor reports `renderFps 4` with `renderGapP50Us 495900`. | Correct behaviour, not a regression. LVGL renders only what is invalidated, and a real MAP sensor sitting at constant atmosphere invalidates nothing. The guard had only ever been run in demo mode, whose synthetic sweep changes the value every tick. | Run the cadence guard **in demo mode**, then switch back to the real sensor. The guard measures the display pipeline, which is what it was written to protect; the sample source is irrelevant to it. | Any acceptance criterion phrased in terms of `renderFps` must state the sample source. Written against real sensors it sends the next agent hunting a display regression that does not exist. |
| Every dashboard error flashed for a quarter of a second | Wi-Fi connect/scan failures and gauge-range validation messages appeared in the shared `#errorBox` and were gone before they could be read. The MAP calibration panel had already worked around it with a panel-local `#calStatus` line. | `pollState()` called `showError("")` on **every successful** `/state` sample. On HTTP fallback that is `POLL_FRAME_MS` = 250 ms, so the poll loop retracted errors it never raised. Measured on the host mock against the pre-fix `HEAD`: an identical validation error survived **223 ms**. | Every write to the box carries its source. `showError(msg, source)` / `clearError(source)` take `ERR_LIVE` (unattended telemetry: `pollState`, WebSocket frames) or `ERR_USER` (a gesture's outcome), and a producer clears only what it raised. `ERR_USER` outranks `ERR_LIVE` in both directions, so a poll can neither overwrite nor erase a message being read; `showOk()` or a click on the box dismisses it. Boot's unattended `refreshAll(ERR_LIVE)` stays retractable so a device that is dead at boot does not leave a stale banner once it returns. `updateConnection` and `POLL_FRAME_MS` are untouched. | One convention only: do not add panel-local status elements to dodge the shared box, and do not make all errors sticky. Host mock, both pages: a user error must survive tens of seconds of 4 Hz polling (measured **14.9 s** and **24.4 s**, **0** hidden samples across 106 successful polls), a `pollState`-raised error must clear on recovery (measured appear 47.3 s, clear 50.3 s, in lockstep with the badge), and the badge must read `Live · HTTP 4 Hz` throughout with **0** `Disconnected` samples across 267 samples and 8 WebSocket retry attempts. `tools/mock_server.py` carries `scanFail`/`stateFail` fault injection for exactly this. |
| Vault two-segment invalidation looked cleaner in the host audit but felt worse on the panel | The consolidated 146x34 Vault readout was kept and the needle segmentation was tested independently at 2 and 3 segments. Host RGB565 audit favored 2 segments for isolated seams (12,858 px/cycle, 0 severe / 1 total mismatch) over 3 (11,460 px/cycle, 3 severe / 8 total), but the dirty regions were larger. | A matched hardware fast-sweep run favored 3 segments: 60/60 min/median FPS, 17.162 ms median / 20.194 ms max render, 1 over-budget frame, versus 2 segments at 58/60 FPS, 18.222/33.558 ms and 9 over-budget. A later 3-segment run was noisy at 55/59 FPS, 19.564/33.623 ms and 28 over-budget, so the spread is retained rather than hidden or called a regression. | Production keeps `VAULT_NEEDLE_SEGS=3`, the hardware-proven two-triangle raster, and the bounded consolidated six-slot object. Host mismatch counts are correctness evidence, not cadence evidence; rerun both simulator stale-pixel audit and interleaved hardware tails before changing either factor. |
| Vault counterweight-tail trial | The selected three-segment, two-triangle, bounded-readout renderer still draws a 26 px wedge behind the pivot. `VAULT_NEEDLE_TAIL` now defaults to 0 for a reversible visual/performance trial; `-DVAULT_NEEDLE_TAIL=26` restores the measured baseline. | The parameter is shared by wedge geometry, large-jump fan endpoints, and segmented `r_lo`, so draw and partial-refresh invalidation cannot drift. LEN=168, HUB_R=15, all AA/hub padding, CRT, cache, peak marker, and 146x34 readout remain unchanged; the web mirror also starts at the pivot. | Treat tail=0 as a candidate, not a proven optimization. Require separate baseline/candidate RGB565 audits and interleaved hardware fast-sweep rounds, plus no visible pivot gap, residual tail, hub corruption, or trails before keeping it. |
| Docs said 20 Hz, the code had moved to 62.5 Hz | The connection badge read `Live . WebSocket 60 Hz` while the cadence contract and six other places said 20 Hz. Reading the docs first, the badge looked like it was lying about the transport rate. | The badge was right and the documentation was stale. Telemetry stopped being a free-running timer when the push loop became notification-driven: `STATE_WS_PUSH_DECIMATION 1` emits one frame per 16 ms sample, i.e. 62.5 Hz. `STATE_WS_FRAME_MS 50` survives only as the idle *fallback* timeout for a stalled producer, and reads exactly like a 20 Hz period if skimmed. Six doc sites were never updated with the code. | Corrected every rate reference in `AGENTS.md`, `README.md`, `boost_web.c` and `web/app.js` to 62.5 Hz, and named `STATE_WS_FRAME_MS` as a fallback rather than a period wherever it is cited. | The code is the authority on what the hardware does; a documented rate is a claim to verify, not evidence. Before 'fixing' any value to match a doc, read the producer - and treat a timeout passed to `ulTaskNotifyTake` as a ceiling, never a period. Same failure class as the phantom 60 MHz QSPI trial row above. |
| 82 KB of internal DRAM went to an LVGL IRAM option nobody had priced | A BLE spike ran out of internal RAM, and `main` itself was measured at only **13,611 bytes** free internal at peak with a 7,680 B largest block - far tighter than anyone had assumed. | `CONFIG_LV_ATTRIBUTE_FAST_MEM_USE_IRAM=y` costs **82,812 bytes of DIRAM**. That is 2.3x NimBLE's entire runtime cost (36,320 B) and 6x its static footprint, spent on an LVGL speed option whose benefit had never been measured against its price. It also explains the earlier GIF-decoder boot loop: the headroom to absorb 24.5 KB simply was not there. | Not yet changed on `main`. Measured with the option off: internal free goes 13,611 -> 114,023 bytes, and the cadence guard was unchanged (min 57 / median 60 against a min 58 / median 60 baseline) - but only the `arc` face was tested. | Before turning it off, run the cadence guard **per face** in demo mode and compare `worstRenderUs` and `pixelsPerSecond`, not just `renderFps`. Before adding anything to internal DRAM, measure free internal at *peak* - boot-time figures hide the problem entirely. |
| The leaked WebSocket slot, fixed | Row 149's leak: a disconnect could permanently remove a slot from the 3-client pool, so throughput fell 187.5 -> 125 -> 62.5 -> 0 f/s and later clients landed on 4 Hz HTTP fallback until reboot. | `state_ws_get()` released a slot by setting `fd = -1` only. `state_ws_send_done()` clears `inflight` under `client->fd == ctx->fd`, which can never match again once `fd` is `-1`, and allocation needs `fd < 0 && !inflight`. The `payload == ctx` guard could not be dropped either - it is what rejects a stale completion. | One `state_ws_release_locked()` clears `fd`/`payload`/`inflight` together and bumps a per-slot `gen`; `gen` is carried in the send ctx and checked **before** `fd` in the completion, and in every re-check in `state_ws_push`. The close path frees nothing: once `httpd_ws_send_data_async()` returns ESP_OK, `httpd_ws_send_cb` always runs the completion exactly once, so the completion keeps sole ownership of `ctx`/`ctx->payload` and there is no double free. Bumping `gen` is also what makes the slot reusable *immediately*, before an in-flight send completes. | The leak is **probabilistic**, not per-disconnect: it needs the CLOSE to be handled between the telemetry task marking a frame in flight and the completion running. 90 graceful reconnects and 1,200 rapid ones on the broken build leaked nothing; it only reproduces under httpd load. Measured on the broken build with 2 concurrent `/state` pollers: leaked at **96, 104 and 394** churn disconnects, pool showing `fd:-1, inflight:1`. Fixed build, identical load: **0 leaks in 1,320 + 1,200 disconnects**, and 5 consecutive 3-client disconnect/reconnect cycles at **187.7-188.0 f/s** with generations incrementing once per cycle per slot. Test against the pool's own state, not the aggregate rate - see the confound below. |
| The largest single consumer of internal DRAM was a log buffer | Row 147 measured only 13,611 bytes free internal at peak with a 7,680 B largest block. The biggest single item in that pressure was not LVGL, Wi-Fi or NimBLE. | `s_logs[BOOST_LOG_CAPACITY]` was static `.bss`: `.bss.s_logs 0xa8c0` = **43,200 bytes** of internal DRAM, larger than NimBLE's entire runtime cost and larger than the free internal RAM the firmware had. It is written at 12.5 Hz and read on demand for CSV export - no DMA, no ISR, no latency requirement. | `heap_caps_calloc(BOOST_LOG_CAPACITY, ..., MALLOC_CAP_SPIRAM)` once in `boost_model_init()`. NULL disables logging with a warning instead of failing boot; `boost_model_publish_sample()` and `boost_model_copy_logs()` are guarded, and all access stays under `s_lock`. Ring semantics, capacity, head/count and the read path are unchanged. | Measured on hardware: `.bss.s_logs` **43,200 -> 4 bytes**; internal free at peak **13,611 -> 56,727**, largest block **7,680 -> 43,008**, minimum-ever-free **9,535 -> 52,659**. CSV export **after the ring wrapped**: 1,800 rows spanning 143.9 s, strictly increasing `uptime_ms` at an 80 ms median (12.5 Hz), `badTimestampCount 0`. Cadence guard unchanged at min 57 / median 60 (demo mode, `dyno-cell`). Verify after a wrap, not before - a ring that works only until it wraps is the classic failure here. |
| The user's LAN dashboards are part of the pool under test | Every WebSocket throughput series decayed 187 -> 125 -> 62 whether the firmware was fixed or broken. | Two dashboards were open on the LAN. They hold slots indefinitely and re-race within ~1 s of a reboot, so any run that leaves a slot free for more than a few milliseconds loses it - which reads exactly like a leak in the aggregate rate. A separate artifact inflated rates the other way: httpd runs `max_open_sockets=7` with `lru_purge_enable`, so an aggressive reconnect loop evicts the very sockets being measured, and a socket left undrained buffers frames that then land inside the measurement window (a 2-client stage read 94 f/s per client instead of 62.5). | Measure the firmware's own slot state, and distinguish `fd == -1 && inflight == 1` (leak) from `fd >= 0` held by a foreign client (confound). Hold every slot continuously and roll one socket at a time; race all three at once after a restart; flush buffered frames before timing. | Never infer pool health from aggregate frames/s alone. Per-client is **62.5 Hz**; the pool total is **187.5 f/s** and each lost slot costs exactly 62.5 (measured 187.9 / 125.3 / 62.6 for 3 / 2 / 1 clients). |
| The 82 KB IRAM option buys nothing measurable | `CONFIG_LV_ATTRIBUTE_FAST_MEM_USE_IRAM=y` held **82,812 bytes** of DIRAM to keep LVGL's rasteriser in IRAM. Nobody had ever measured what that bought. | Set in the very first commit with no rationale; every later analysis priced the *cost* and assumed the benefit. | Measured on hardware, all four faces, demo mode, 3 verified WebSocket clients, three 30 s runs per face per build (six for `night-city`), each from a **fresh boot**: nothing moves outside run-to-run spread. `renderFps`, `worstRenderUs` and `pixelsPerSecond` flat on every face, including the `big-digit` full-screen recolour (56.0 ms vs 56.9 ms against a 2.1 ms spread) where it should have helped most. Only signal: `night-city` `framesOverBudget` 15 -> 16/s. Turned off in `sdkconfig.defaults`. | Two corrections to the record. (1) The BLE spike's "114,023 B with the option off" **also had both Wi-Fi IRAM options off**; this option alone gives a figure re-measured after rebase, below. (2) Its "WS frames/s 133.1 vs 200.1" is **not** memory pressure - both builds sit at the 187.4 f/s contract ceiling (62.5 Hz x 3). 133 was two live clients, not three, caused by the slot leak fixed two rows above. Withdraw the telemetry-throughput argument. |
| A leaked WebSocket slot made every back-to-back measurement non-comparable (found independently, already fixed above by the time this branch reached `main`) | First 3-client connection after boot works; every later attempt has the third client accept the HTTP 101 then close, with `W httpd_uri: httpd_uri: uri handler execution failed` on serial. Aggregate telemetry sticks at ~125 f/s instead of ~187.5. | `state_ws_get()` handles CLOSE/recv-error by setting `fd = -1` without clearing `inflight`; `state_ws_send_done()` only clears `inflight` when `client->fd == ctx->fd`, which can never match again. Slot allocation needs `fd < 0 && !inflight`, so the slot is dead until reboot. | Found and characterised during the IRAM A/B on this branch's original base (`c18339c`), before the fix above landed on `main`; every run in that A/B rebooted between measurements to sidestep it rather than fix it, so the A/B numbers are unaffected. | **Reboot between WebSocket measurements and verify how many clients are actually receiving**, or you are comparing different client counts. The pool is also shared with any dashboard open on the LAN, which reconnects within ~1 s of a reboot and races for the last slot - so verify, do not assume. Reading COM3 resets the board unless DTR/RTS are driven low *before* opening the port. |
| Kconfig symbols that do not exist are accepted in silence | `CONFIG_BT_LE_*` entries were added to `sdkconfig.defaults` during the BLE spike and had no effect whatsoever. | Those symbols do not exist on ESP32-S3. The build system accepts unknown symbols in a defaults file without warning, so the config read exactly as intended and did nothing. Separately, `CONFIG_BT_CTRL_BLE_MASTER` is not the central-role switch its name suggests - its help text is "Enable BLE connection feature", and with it off you get an unconnectable peripheral advertising `CONNECT:0`. | Verify every new Kconfig symbol appears in the generated `sdkconfig` after a build, not just in `sdkconfig.defaults`. | Same failure class as the phantom 60 MHz QSPI trial: a config line is not evidence of a config taking effect. Grep the generated `sdkconfig`, and confirm the behaviour on hardware. |
| The LVGL IRAM option landed on `main`, with a real (small) cost found on rebase | Merging `spike/lvgl-iram-benefit` needed three gaps closed against today's `main`: the RAM headline predated the PSRAM log-ring move, `big-digit`'s clean-network re-run was n=2 and LAN-contaminated, and GIF playback had never been tested with the option off. | The branch's own `96,611 B` free-at-peak figure was measured before the log ring left internal `.bss`, so it no longer described `main`. The `big-digit` gap and the GIF gap were both explicitly named as open risk in the branch's own research doc rather than glossed over. | Rebased onto `09e0369` (clean, both sides' ledger rows kept). Re-measured RAM the same way as the original A/B: minimum-ever-free **52,659 -> 135,607 B** (delta 82,948 B, matching the option's previously-isolated 82,812 B cost). Re-ran `big-digit` clean, n=6/build, interleaved across two rounds to rule out time drift: **53.7 ms vs 57.4 ms median `worstRenderUs`, ranges non-overlapping (52.6-55.4 vs 56.1-59.7)** - a real ~3.7 ms/7% cost, reported honestly, but not gating because it doesn't touch the `dyno-cell`/arc cadence guard and `big-digit` was already 3x over its render budget on both builds. Uploaded a synthetic 128,150 B animated GIF and verified upload/commit/playback/reboot-survival/delete on both builds: clean, no `ESP_ERR_NO_MEM` or `send color data failed`, and IRAM=n showed *more* internal headroom at GIF alloc time (194,055 B vs 111,111 B at the same boot stage), not less - no regression. | See `docs/research/2026-07-26-lvgl-iram-benefit.md` update section for full numbers. A "no measurable benefit" verdict from one A/B round is not the same as "no cost anywhere" - the clean re-run found a real effect the original n=3/contaminated-LAN run could not resolve. Judge a finding against the project's actual gated invariants (here, the `dyno-cell` cadence guard), not against every metric that moved. |
| Region double-buffering spike: feasible, built, real but modest cost | The row above ("TE sync engages but does not stop the needle tearing") left the fix as "region double-buffering, a larger change" with no numbers behind it. Needed a go/no-go before building: rasterise the whole dirty region off-screen, then DMA it back-to-back inside one vblank, so transfer - not rasterisation - races the scan. | GDMA still cannot stream from PSRAM (row 1), so a memcpy into internal DMA-capable memory per strip is mandatory. Measured on hardware (`spike/region-double-buffer`, before writing any feature code): per-strip DMA transfer at 80 MHz QSPI averaged **999.0 us** (200 back-to-back transfers, 18,640 B, min 997/max 999 us) against a ~0.47 ms theoretical figure; PSRAM->internal memcpy for the same block averaged **50.9 us** (~349 MB/s). For the needle's 205-row (~191 KB) band, rounded to 11 strips: 11 x (999.0 + 50.9) = **11,548.9 us** against a measured `tePeriodUs` of **16,753 us** - **31% margin**. Rasterisation alone (measured by making the flush hook skip the real write and reading `worstRenderUs` with teSync off, 30 s window) had a **13.6 ms median**, also under the period. Verdict: GO. | Built behind a new runtime toggle `regionDBuf` (persisted, default **off**, same shape as `teSync`), wired through the adapter's `custom_draw_bitmap` hook (`boost_display.c`): each strip is copied into a PSRAM staging canvas and the real transfer deferred to `LV_EVENT_RENDER_READY`, which pushes the accumulated region back-to-back after one TE wait. First version tracked a single min/max y union and, on hardware, that dragged every untouched row between disjoint dirty areas along with it (the vault needle and a digit-slot label can invalidate in the same tick at unrelated y): `pixelsPerSecond` inflated ~7.5x (437,764 -> 3,269,456 median) and the cadence guard fell to min 30/median 53-54 on `vault-tec`. Fixed by tracking up to 8 disjoint (x,y) spans per cycle instead of one union, narrowing both the row range and the column width per span. After the fix: `dyno-cell` cadence guard **unchanged**, min 57/median 60 across 3 fresh boots with the feature ON (matches the min 57/median 60 reference exactly). `vault-tec` (the target face) costs a real but modest amount: min 49/median 57 -> min 42-43/median 56 across 3 fresh boots; `worstRenderUs` median 23,056 -> ~20-21k us (no worse, sometimes better); `framesOverBudget` median 5 -> 4-5 (flat); `teWaits` continues to track `renderFps` 1:1 with `teTimeouts` 0 throughout. No `ESP_ERR_NO_MEM` or `send color data failed` on any boot, toggle, or theme switch across the whole session. | Two caveats, found during measurement/verification, not assumed away: (1) the measured write rate (999.0 us / 20 lines = 49.95 us/row) is **slower** than the panel's own measured scan rate (16,753 us / 466 rows = 35.95 us/row), so a single wait-then-blast burst only has unconditional margin over the scan when the region's top row is roughly row 86 of 466 or lower - a region starting nearer the top does not have a proven guarantee from this mechanism alone (the CO5300's `set_tear_scanline`, already stubbed as `BOOST_LCD_TE_SCANLINE`, is the next lever if that turns out to matter in practice). (2) Panel snapshots come from the framebuffer, not the glass, and cannot contain a tear by construction; nothing in this session photographed the physical panel, so "the race that caused tearing is removed by construction" is the honest claim from the measured numbers, not "tearing is visually confirmed fixed." Any reviewer relying on this feature for a real tearing complaint should watch the glass, not `/api/v1/state`. **RETRACTED, see the row immediately below: the 999.0 us / "write is slower than scan" figures in this row do not reproduce and are superseded by 575.9 us / write-is-faster-than-scan.** |
| A measurement nobody can re-run is not evidence | The README's QSPI figure was corrected **three times** and was wrong every time. Revisions 1-2 quoted arithmetic as measurement (the phantom 60 MHz clock, then 80 MHz). Revision 3 over-corrected in the opposite direction: it reported a genuinely-taken **999.0 µs per 18,640 B strip / 18.7 MB/s / 23.3 ms per frame** and concluded "the hardware does not deliver the arithmetic". That figure does not reproduce - this is the same 999.0 us figure the region-dbuf feasibility row above relied on. | The 999 µs measurement was never committed as inspectable code — only described in a commit message — so its methodology could not be replicated and the cause of the discrepancy is still unknown. A model was then built on top of it (fixed overhead ≈533 µs/transfer) which fit three independent numbers to within 0.3 ms and was **entirely spurious**, because its inputs were wrong. A good fit is not evidence when the measurement feeding it is unverified. | Re-measured with a committed harness: size sweep 932 B → 37,280 B × 3 clocks × 40 reps, completion-to-completion deltas. Link rate is **39.4 MB/s at 80 MHz (98.5% of theory)**; fixed overhead is **106 µs/transfer, clock-independent**; a full frame is **13.6 ms (≈74 FPS)**. Clock confirmed at the requested rate by `spi_device_get_actual_freq()` *and* by throughput tracking theory across 80/40/20 MHz. Reproducible at quiet boot and under full Wi-Fi+LVGL+HTTP load (575.9 vs 578.1 µs). | **Commit the harness before quoting its numbers.** The correct lesson from the earlier rows is not "measure instead of calculating" — a measurement that cannot be re-run is worse than arithmetic, because it carries unearned authority. Also: `esp_lcd` `draw_bitmap()` is *functionally blocking* here — `tx_param()` drains all in-flight transactions before its own transmit, and CASET fires every strip, so `trans_queue_depth=4` never exceeds depth 1. Do not assume transfers pipeline. |
| The panel is clocked 60% over its documented maximum | `BOOST_LCD_PCLK_HZ` is 80 MHz (`main/boost_display.c:45`). The CO5300 datasheet §6.4 p.107 specifies write-cycle `TSCYC` MIN = 20 ns — a documented ceiling of **50 MHz**. The vendor driver's own default macro is 40 MHz. | This repo overrode the vendor default without reference to the AC timing table. It has been reliable across extensive bench testing, and throughput tracks theory at 80 MHz, so the silicon evidently tolerates it. | No change made — the risk is latent, not observed, and lowering the clock would cost real full-frame time. Recorded so the decision is explicit rather than accidental. | **Do not raise the clock above 80 MHz; there is no documented headroom and none of the measured evidence supports the bus being a constraint anyway.** Note the deployment is an automotive dashboard: out-of-spec timing margins shrink at temperature extremes, and bench stability at ~22 °C does not establish stability at −20 °C or heat-soaked. Only SPI-transaction success and throughput were verified — no optical/pixel-level check has ever been done at 80 MHz vs a spec-legal clock. |
| Region-dbuf's 32 ms worst case was a missed-vblank tax, and eliminating LVGL's own strip copy is architecturally blocked | Branch `fix/region-dbuf-worst-case`, off `spike/region-double-buffer`. Two hypotheses to test: (1) can LVGL rasterise straight into the PSRAM canvas, removing the LVGL-strip→canvas memcpy ("copy 1")? (2) is the ~32 ms `worstRenderUs` tail (≈2x the 16.7 ms TE period) a missed-vblank artefact of `region_dbuf_writeback()`'s single unconditional wait? | (1) `display_manager_validate_tearing_mode()` (`managed_components/espressif__esp_lvgl_adapter/src/display/display_manager.c:1814-1864`) only accepts `tear_avoid_mode` NONE/TE_SYNC for `ESP_LV_ADAPTER_PANEL_IF_OTHER` (this QSPI panel); `DOUBLE_DIRECT`, the only path to `LV_DISPLAY_RENDER_MODE_DIRECT` (`display_manager_pick_render_mode()`, same file:1869-1881), is rejected outright — `esp_lv_adapter_register_display()` returns `NULL` (`display_manager.c:219-221`) and `boost_display_start()` fails. A hard block confirmed by reading the vendored source; no hardware needed to know it fails, and none was spent finding out. (2) `region_dbuf_writeback()` waited via `te_wait_for_vblank()`, whose only "go now" test was a fixed 2 ms freshness window designed for the OLD per-flush path (write starts at row 0, right after `RENDER_START`). Region-dbuf's burst starts wherever the dirty region's top row is, and only after ~13.6 ms of rasterisation — so the 2 ms window is stale almost every cycle, forcing a wait for the *next* edge, up to a full ~16.75 ms period on top of the raster time. | (1) Not built — genuine blocker, falls back to optimising copy 1 per the task's own instruction rather than a behind-the-adapter's-back hack (rejected for the same reason the phantom-60-MHz and `bsp_display_brightness_set` rows above were burned: editing a vendored component's behaviour without its own bookkeeping knowing about it). `te_draw_bitmap_cb` now collapses the per-row memcpy loop into one `memcpy` when the strip is full panel width (source and destination both contiguous in that case) — real but modest, not measured in isolation on hardware (the adapter block makes the original idea moot regardless). (2) `te_wait_for_region(top_row, bottom_row)` replaces the single fixed-window check with two provably-safe conditions derived from the corrected write-vs-scan numbers (write ~31.3 us/row incl. memcpy vs scan 35.95 us/row, i.e. **faster**, not slower as the retracted 999 us figure implied): skip the wait if the estimated scan position is **before** the burst's top row (write starts ahead and a faster writer never falls behind) **or already past its bottom row** (scan won't re-enter the dirty rows until its next pass, so the burst's new data lands cleanly on that next pass). The genuinely unsafe middle case — scan currently inside the dirty range — still falls back to waiting for the next edge, i.e. never worse than before. Scan position is estimated from elapsed time since the last TE edge and the panel's own measured period (`s_te_row_time_ns`), with a conservative 20-row margin. New `teSkips` counter (`/api/v1/state`) makes the skip rate directly observable rather than inferred. | **Measured on hardware** (`tools/bench_region_dbuf.py`, committed before quoting these numbers, 3 interleaved OFF/ON rounds, reboot between every run, `vault-tec`, demo mode, pixelShift controlled off to remove an unrelated confound, 14 s settle + 20 s sample per run): `teSkips`/`teWaits` **537-566 / 1016-1054 (≈52-54%)** of cycles now provably skip the wait entirely (0 by construction when `regionDBuf` is off). `worstRenderUs` median **20.4-28.3 ms ON vs 23.2-23.9 ms OFF** (mixed — lower in 2 of 3 rounds, higher in one), max **33.0-35.2 ms ON vs 30.3-33.8 ms OFF** — the worst-case gap over OFF narrowed (a first cut with only the "early" skip condition measured max 33.0-35.4 ms at a lower 41-42% skip rate) but did **not** close: cycles whose dirty region sits near the top of the panel can still land in the unsafe middle for the whole raster+wait duration, and the true worst case is not eliminated, only reduced in frequency. `teTimeouts` stayed **0** throughout every run in this session (TE hardware itself never faulted). `dyno-cell`/arc guard, 3 fresh boots each: OFF **min 57/median 60** (matches the historical reference exactly); ON **min 56 in 3 of 4 fresh-boot trials, 57 once, median 60 in all** — a small, reproducible-but-not-absolute 1 FPS dip specifically with `regionDBuf` ON that was not present in the pre-fix `spike/region-double-buffer` baseline (min 57/median 60). Root cause not isolated (would need bisecting Fix 1's copy-1 change from Fix 2's wait logic and was not done this session); flagged rather than smoothed over. No `ESP_ERR_NO_MEM` or `send color data failed` across ~15 reboots this session. |
| Fast-motion cadence isolated: real but not fixed this session, and the `dyno-cell` min-56 dip does not reproduce | Two asks, branch `spike/fast-motion-cadence` off `fix/region-dbuf-worst-case`. (1) The user visually confirmed `regionDBuf` ON removes tearing (non-negotiable, not to be traded away) but then noticed the needle *looks* low-frame-rate during fast motion — the aggregate `dyno-cell`/demo cadence guard (which mixes the demo sweep's fast and slow segments) cannot see this. (2) The row below left the `dyno-cell` min-56-in-3-of-4 dip flagged, not root-caused. | (1) Two independent isolation methods, both committed in `tools/bench_fast_motion.py` before quoting numbers: a controlled constant-slew triangle sweep (`demoFastSweep`, new transient/unpersisted toggle in `boost_sim.c`, off by default, no effect unless `demoMode` is also on) sustained at **9.789 psi/s**, the organic demo's own measured peak *trend* `|dpsi/dt|` (noise terms excluded) — not an invented faster case; and an empirical cross-check that polls the unmodified organic waveform and buckets the once-a-second `display.*` telemetry by the `|dpsi/dt|` actually observed in that same wall-clock second (smoothed over 0.35 s to reject the sim's own ~2.75/4.95 Hz flutter terms). (2) Reproduction attempt: 8 fresh-boot `dyno-cell`/demo trials on the current build (a7b01c6 + this session's unrelated `boost_sim.c` changes only — no display-path edits), 4 with `pixelShift` off and 4 with it on (matching the board's persisted default), each the standard 30 s `check_display_cadence.py` soak. | (1) **Controlled sweep**, `regionDBuf` ON vs OFF (OFF is reference-only — it tears, per the user's own confirmed observation, and is never left on): `renderFps` median 59 (min 52) vs 60 (min 58); `renderGapP50Us` median 16,900 vs 16,000 us; `worstRenderUs` median **31,684 vs 21,322 us** — regionDBuf's own mechanism costs roughly a doubled TE period on top of the OFF baseline specifically under sustained fast motion; `teSkips`/`teWaits` 395/1,105 (36%) vs 0/1,130 skip-not-possible baseline. **Organic cross-check** (pixelShift forced off — a 90 s-scale run reliably straddles the 90 s `pixelShiftSec` full-repaint otherwise, the exact confound this ledger already warns about): fastest quartile of seconds (mean 8.05 psi/s) shows `renderFps` median **41.5 (min 35)**, `framesOverBudget` median **21.5/s**, vs the slowest quartile (mean 0.75 psi/s) at median 55.0 (min 33), 6.5/s — materially worse than the controlled sweep at a similar instantaneous rate, most likely because the organic waveform's high-frequency flutter (`0.18·sin(17.3t)+0.08·sin(31.1t)`, omitted from the clean sweep on purpose) adds direction-reversal invalidation events on top of the trend motion that the clean triangle never produces. Both methods agree in direction (fast much worse than slow) but not in magnitude; reported as found, not smoothed over. Root cause, read from the code, not just inferred from the numbers: `te_wait_for_region()`'s early/late skip test uses the **union** of `top_row`/`bottom_row` across every span in the cycle (`region_dbuf_writeback()`'s loop just before calling it), not each span's own range. The vault needle and the six digit-slot labels sit far apart on screen and both invalidate on almost every tick once psi is moving at all (the needle's own 0.35° gate is crossed within one 16 ms tick at any of the organic waveform's observed speeds), so during fast motion the combined row range is wide on nearly every cycle — which is exactly when the early/late proof is least likely to hold, forcing a real wait. A wait landing after the ~13.6 ms raster floor has already elapsed pushes the cycle to roughly a second TE period (~33.5 ms), which reads as a stutter rather than a rate drop, matching the user's report. **Not fixed**: the fix (score the proof against actual per-span transfer time, `Σ span_height_i × row_time`, instead of the naive `bottom_row − top_row` union — provably *at least* as safe, since it only makes the model of how fast the write finishes more accurate, never assumes it finishes faster than it will) touches the exact safety proof the user's own eyes verified tear-free, and re-verifying that after a change needs a fresh visual check this session had no way to obtain. Also investigated and not shipped for the same reason: the RAMWRC (`0x3C`) per-chunk command reduction named in this task's own brief — real (~70 µs/chunk, ~3.5% of the 16.75 ms budget across a typical ~8-chunk cycle) but requires bypassing the vendor CO5300 driver's own `esp_lcd_panel_draw_bitmap()` with hand-rolled `s_panel_io` transactions, and a wrong coordinate/opcode there would show up as on-glass pixel corruption that the framebuffer-based debug snapshot **cannot** catch (it never touches the wire path being changed) — same "needs the user's eyes, not a snapshot" gate as (1) above, and smaller expected payoff than the wait-conservatism fix regardless. (2) **`dyno-cell` min-56 dip: did not reproduce.** All 8 trials read **min 57-58, median 60-61** — at or above the historical min 57/median 60 reference, both with `pixelShift` on and off. The leading confound hypothesis (`pixelShift`'s 90 s full-repaint landing inside the measurement window) is ruled out directly: uptime at guard-start was consistently ~4.7 s and at guard-end ~40.6 s across all trials, so the 30 s window never reaches the 90 s `pixelShiftSec` boundary regardless of pixelShift state. Conclusion: the original min-56-in-3-of-4 result was itself reported as "not absolute" (57 once), and 8 further trials finding zero repeats of it is stronger evidence for **sampling noise in a minimum-of-~25-buckets statistic** (a single unlucky one-second bucket is enough to move a minimum by 1, and this session could not force one back into existence) than for a systematic regression. Judged acceptable; not fixed, because there was nothing reproducible to fix. | Both isolation methods and the reproduction attempt are hardware-measured and committed (`tools/bench_fast_motion.py`); re-run them rather than trusting these numbers. The two identified next levers (per-span TE-wait timing; RAMWRC) are real candidates for a future session **that has the user's eyes on the physical panel available**, not for one that does not — this session declined to ship either rather than claim a display-path fix it could not verify. `regionDBuf` was left ON, matching the non-negotiable tearing fix; nothing in this session traded cadence for it. Board restored to `vault-tec`/demo/teSync/regionDBuf/pixelShift all ON, `demoFastSweep` OFF, matching the state found at session start. |
| The per-span TE-wait fix shipped and flashed for the user to verify by eye, but does not move the fast-motion numbers | Coordinator asked for the previously-identified per-span fix (row above) to actually be built and put in front of the user at the bench, since "needs the user's eyes" is resolved by giving them a build to look at, not by declining to ship anything. Branch `spike/fast-motion-cadence`. RAMWRC explicitly excluded from this round (kept to one variable). | `te_wait_for_region_spans()` replaces `te_wait_for_region()`: same early/late proof, scored per span (using each span's own y0/y1) instead of once against the whole cycle's `[top_row, bottom_row]` union - see the block comment on the function for the full argument, including where it is and is not a strict superset of the old union proof. First cut converted an earlier span's row count directly 1:1 into "scan rows" for the EARLY projection - equivalent to assuming the WRITE moves at the SCAN's slower ~35.95 µs/row rate instead of its own measured ~31.3 µs/row. Hardware A/B caught this immediately: fast-sweep `renderFps` median fell from 59 (old union) to 54-57 across repeated runs of the first cut, and `teSkips` went DOWN (368 vs 395), the opposite of what the fix was supposed to do - an ordinary tall single span (the vault needle near vertical is ~150-200 rows) alone was enough to inflate the projected scan position past what the union check needed. Fixed by introducing `BOOST_REGION_DBUF_WRITE_ROW_TIME_NS` (32 µs/row, rounded up from the measured ~31.3 µs/row) for the elapsed-time estimate, converted to scan-row units for the actual comparison; both the row-count and chunk-overhead terms now round UP (ceiling division), since under-estimating elapsed time is the unsafe direction for an EARLY proof. | **Corrected fix vs. the union baseline, interleaved hardware A/B** (rebuild + reflash between every arm - PowerShell `idf.py -p COM3 build flash` directly, not wrapped through Bash's `powershell.exe`, which corrupts the IDF Python-environment detection and fails the build outright; `tools/bench_fast_motion.py sweep`, 3 rounds, `demoFastSweep` at 9.789 psi/s, pixelShift controlled off): union **59/59/55** vs per-span **59/59/55** median `renderFps` per round (paired, essentially identical run-for-run - the fast-sweep's own fixed 4.92 s period aligning differently with a fixed-delay sample window each reboot is itself a real source of ±4-5 FPS median swing between *identical* builds, confirmed by measuring the union build against itself twice), `worstRenderUs` median 30,979/32,387/29,408 vs 29,340/31,635/30,217 (paired, no consistent direction). Averaged `teSkips` across the 3 rounds: union 360, per-span 381 (**+5.8%**) - a real, if modest, increase in the mechanism doing what it was built to do. `framesOverBudget` summed: union 153, per-span 148 (**-3%**). **Organic cross-check, fastest quartile**: `renderFps` median 42.0 (min 30) vs the pre-fix baseline's 41.5 (min 35), `framesOverBudget` median 19.0 vs 21.5 - again no clear win outside noise. `dyno-cell`/arc guard unaffected: 3 fresh boots, min 56-58/median 60, matching the noise band already characterised in the row above (not a new regression). No `ESP_ERR_NO_MEM`, `send color data failed`, or `teTimeouts` across the whole session. **Conclusion, reported as found rather than chased further**: the per-span fix is a real, verified, hardware-measured improvement to the underlying skip-rate mechanism, and is not a regression (unlike its first cut) - but it does not deliver a measurable improvement in the fast-motion cadence numbers that motivated it, in either isolation method. The likely reason: both the controlled sweep (a full-range triangle, which spends much of its time with the needle alone spanning a large single tall region) and typical organic fast motion are apparently dominated by single-span or adjacent-span cycles, where this fix is mathematically identical to the union version it replaces (`rows_before` is 0 for the first span); the specific "needle and a far-away digit label both invalidate at once, with a wide gap between them" scenario this fix targets is real (it is what `region_span_add`'s own disjoint-span tracking exists for) but is not, on this evidence, the dominant cost of fast-motion stutter. The binding constraint identified in the row above - rasterisation at ~13.6 ms against a 16.75 ms budget, worsened by whatever *does* force a real per-cycle wait - stands. | Flashed and left running on `vault-tec`/demo/teSync/regionDBuf/pixelShift all ON, `demoFastSweep` OFF (transient, does not survive reboot even if left on - confirmed off here regardless). **What needs the user's eyes now, framed as two separate questions**: (a) is the panel still tear-free with this build, exactly as it was before - this change touches the same safety proof the earlier tear-free confirmation was made against, so that confirmation should be treated as needing a fresh look, not assumed to still hold; (b) does fast motion look smoother than before, or unchanged - the telemetry here says "unchanged, and safely so," and if the user's eyes agree, that is the honest answer to report, not evidence to explain away. The Settings UI `regionDBuf` checkbox (`web/settings.html`/`web/app.js`, already on this branch via `6c99426`) lets them toggle it live to compare against OFF, keeping in mind OFF is known to tear and is for A/B contrast only, not a state to leave the board in. |
| Vault needle raster cost, not TE wait, was the dominant fast-motion bottleneck | The corrected per-span TE proof changed skip statistics but not cadence. Temporary phase telemetry then isolated `RENDER_START`→`RENDER_READY`: baseline raster p50 was 9.12/9.43 ms; suppressing only the needle draw while preserving its invalidation/transfer geometry reduced it to 5.58/6.18 ms and reached 60 FPS. Component arms showed the tapered wedge was primary (hub-only arm 6.84 ms/60 FPS) and the hub secondary (wedge-only arm 8.07 ms/59 FPS). Source inspection found `draw_vault_needle()` submitted three opaque triangles although the first two already tile the convex A-B-C-D trapezoid; the third A-B-D triangle added no geometry but still paid LVGL's mask/raster/blend path. | Removed only the redundant third triangle. Same-firmware interleaved runtime A/B, identical firmware and dirty geometry: two triangles median FPS **60/59**, raster p50 **8.25/9.58 ms**, over-budget totals **7/17**; legacy three triangles **56/55**, **9.50/10.84 ms**, **104/116**. Temporary profiling modes/metrics were removed before ship after review found they added API/memory/reproducibility surface. Final production build: controlled sweep min/median **58/60**, `worstRenderUs` median **17,949 us**, 8 over-budget frames; organic fastest quartile median **55 FPS**, 6 over-budget vs historical 41.5/21.5, with a single min-21/max-118.9 ms outlier retained rather than hidden. A fixed-hundredths-0 arm cut pixels/s about 10% but did not improve cadence, since tenths still changes every fast frame; not shipped. | Preserve the exact two-triangle split and tight three-segment invalidation. Do not reintroduce overlap "to hide a seam" without same-firmware A/B: static RGB565 snapshot and image inspection found no seam, pinhole, gap, malformed tip/base, or hub corruption. Direct RGB565 writes from `LV_EVENT_DRAW_MAIN` are prohibited: two async LVGL software draw units can race callback writes; any future fast raster path belongs inside draw-unit task execution. Final flashed state: `vault-tec`, demo/TE/regionDBuf on, fast sweep off; serial showed no stack overflow, Guru Meditation, `ESP_ERR_NO_MEM`, send-color failures, or TE timeouts. |
| `dyno-cell` was the last face still re-rasterising its static art every frame | Row 126 already showed `dyno-cell` at 0 dropped-frame gaps/s (best-paced face) yet row 117/599 still named `arc` the worst `worstRenderUs` offender once vault/hud were cached - its unfilled 270deg track, zero notch and five scale numerals were live `lv_arc`/`lv_line`/`lv_label` objects, so any wedge-invalidated rectangle that overlapped their bounding boxes forced a full re-stroke of the ring. | Same class of bug as the vault/hud row above: static furniture was drawn from vector primitives inside the live invalidation path instead of once. | `paint_arc_background()` bakes the ring, zero notch, numerals and the never-changing "PSI" mark into a PSRAM `lv_canvas` at scene build, reproducing each live object's exact `lv_draw_arc`/`lv_draw_line`/`lv_draw_label` call (same radii, same `lv_text_get_size` measurement) so the wedge invalidation and arc geometry are byte-for-byte unchanged. `boost_gauge_apply_config()` dropped arc's incremental `refresh_zero_notch()`/`refresh_tick_labels()` fast path (those objects no longer exist) and now takes the same `destroy_scene()`/`build_scene()` rebuild vault/hud/bigdigit already used, which is what repaints the cache when range/zero-angle change. A failed PSRAM allocation warns and skips the cached art, matching vault's degrade path rather than adding a second convention. | Host audit (`--theme dyno-cell --seconds 25`) unchanged before/after: flushed px/cycle mean 10519 max 36488, 0 severe mismatches - confirms geometry/invalidation untouched. Hardware, demo mode, 3 fresh boots, 30 s `/api/v1/state` polling windows: `worstRenderUs` max 58.4-63.7k / mean ~24-25k us before -> max 43.8-51.3k / mean ~22.7-23.1k us after; cadence guard min 56-57 -> 58-59, median 60 unchanged both sides. Runtime range change (`PUT /api/v1/config`) verified live via `/api/v1/debug/snapshot`: numerals and zero notch position updated with no reboot. One caveat: `lv_draw_label` queues tasks and only rasterises them in `lv_canvas_finish_layer()`, so a loop-local text buffer needs `dsc.text_local = 1` (copy) rather than a raw pointer - the first attempt silently rendered no numerals at all because the stack buffers were long reused by the time the canvas dispatched. |
| Dyno Cell zero notch disappeared behind the moving value arc | The arc background-cache change above baked the zero notch into the background canvas, then created the live value arc above that canvas. The geometry remained correct, but the object order inverted the original visual contract whenever the colored wedge reached zero. | Caching changed draw order even though it preserved coordinates. | Keep the expensive track and labels cached, but restore the tiny zero notch as a live `lv_line` created after `s_arc_value_canvas`. | On glass and in a debug snapshot, the zero notch must remain fully visible over both vacuum and boost wedges; rerun the Dyno Cell cadence guard because the live overlay intersects the moving arc. |
| Night City showed only a moving arc blob when selected during vacuum | Scene rebuild could render before the first HUD sample. That sample committed its negative endpoint without invalidating the complete zero-to-value span, so subsequent endpoint-only invalidations clipped the full arc draw until a zero crossing forced both spans dirty. | On the first HUD sample, invalidate the complete zero-to-current span after committing the fill state. Angle mapping was already correct. | Host audit switched from an already-negative reading, pumped the rebuild before the first HUD sample, and reported 104 comparisons / 0 stale pixels. Keep that ordering in the audit. |
| Night City gradient fill is measurably slower, but is not the whole 40 FPS bottleneck | The user observed that Night City did not look like 60 FPS, especially with gradient fill enabled. A reproducible three-round fresh-boot hardware A/B (`night-city`, demo mode, pixel shift off, 10 s settle + 20 s sample per arm) measured gradient OFF at min/median **30/40 FPS** in every round and ON at **29/39, 29/39, 29/38**. Across-round medians: `worstRenderUs` **31.8 vs 32.0 ms**, `framesOverBudget` **11 vs 13/s**, `pixelsPerSecond` **0.688 vs 0.735 Mpx/s**. No TE timeouts. | `update_hud()` detects changes between 24 RGB565-quantized endpoint colours. At each crossing it correctly invalidates both complete old and new zero-to-value spans because `draw_hud_fill()` paints the entire fill one solid endpoint-derived colour. That adds work, but gradient OFF still reaches only 40 FPS, so disabling it cannot solve the larger Night City cost. | Added `tools/bench_hud_gradient.py`, which reboots each arm, asserts Night City/demo/gradient state, controls pixel shift off, records pacing/work metrics, and restores the initial board configuration. No renderer change yet: fewer steps trade cadence for more visible colour jumps, while a true fixed spatial gradient changes the visual semantics and needs its own stale-pixel-safe design and hardware A/B. | Do not describe `hudGradient` as a free or per-pixel gradient: it is a quantized whole-fill recolour. Optimize against `worstRenderUs`, `framesOverBudget`, and `pixelsPerSecond`, not FPS alone; preserve the first-sample full-span invalidation and committed draw/invalidate state. |
| Night City's chromatic ghost alpha blends were the largest isolated live-render cost | Compile-time diagnostic arms suppressed one operation while preserving update state and invalidation geometry. Gradient-on baseline was **39 FPS / 30.6 ms** median `worstRenderUs`; no ghost **45 / 19.1**, no primary digits **44.5 / 28.8**, no fill arc **43 / 27.6**, no cached background **41 / 28.0**, and all four suppressed **46 / 16.7**. The individual deltas are not additive because two draw units, clipping and TE waits interact, but the ghost result is unambiguous. | `draw_hud_glitch()` submitted two 96 px labels at `LV_OPA_70`. Their shifted edges sit mostly over the static face, so software alpha blending paid destination reads and per-pixel blends twice. | Pre-blend each ghost colour against `theme->face` with `lv_color_mix(..., LV_OPA_70)` and submit both labels at `LV_OPA_COVER`; offsets, font, text, draw order and dirty bounds are unchanged. This is deliberately an approximation, not framebuffer-identical where the ghosts overlap each other or other art. Removed the temporary profiling masks before shipping. Production hardware after the change: gradient ON min/median **27/44 FPS**, OFF **32/46**, no TE timeouts; the low gradient-on min came from one retained outlier rather than being hidden. | The approximation assumes the face is the dominant backdrop. If future art or another live object is placed behind those shifted glyph edges, reassess on glass or pre-render the complete intended stack. The face still misses 60 FPS; next wins are more invasive glyph/arc caching, not another scheduler or QSPI tweak. |
| Tenths cache was incomplete and its lifecycle was unsafe | `lv_font_get_glyph_bitmap()` returns an `lv_draw_buf_t *`, not raw pixels, and mutable PSRAM-backed A8 sources could be retained by asynchronous draw units after publication or teardown. | Completed one immutable **34,786 B** PSRAM readout cache for digits 0-9, decimal, minus, all main labels, and the ghost pass. Stable descriptors are published once; `lv_draw_wait_for_finish()` drains draw units before scene/cache teardown. Allocation failure keeps the source-font fallback. | Simulator Night City audit: **517 comparisons / 0 mismatches**. Hardware: 60 rapid four-theme cycles plus a 60 s Night City soak, no freeze, panic, `ESP_ERR_NO_MEM`, send-color error, or TE timeout. Full-cache hardware benchmark: 3 interleaved rounds, 6 arms, 336 samples; gradient-off aggregate median **42.0 FPS**, `worstRenderUs` **19,378.25 us**, `pixelsPerSecond` **741,639**, zero TE timeouts. Prior tenths-only data was one round (2 arms, 112 samples): **40.75 FPS**, **28,440 us**, **728,238 pixels/s**. This is directional only, not a matched causal A/B. | Cache storage must remain immutable and alive through all callbacks; drain draw units before deleting scene objects or freeing it. Do not use mutable per-frame `lv_image_set_src()` publication. |
| Web mirrors drifted from the completed renderer contract | Night City's chromatic split had become visually negligible at **0.4 alpha / 3 px**. Big Digit ignored `bigDigitStaticBg`, `staticColor`, `colorText`, and `textColor`. | Mirror Night City at **0.7 alpha / 6 px** and make Big Digit consume all four settings. | Host and physical readout verification remained clean; the full-cache hardware result above is the relevant 3-round evidence, while the prior tenths-only comparison is explicitly one round. | Keep web and firmware renderer settings named and behaviorally covered; do not reduce the chromatic split below its visible 0.7/6 px contract or drop any Big Digit setting from the renderer. |
| Night City could not turn unused AMOLED pixels fully off, and its arc looked too light | The face colour was structurally fixed at `#080A08`, and the HUD track/fill width was 10 px in both renderers. | Add persisted `hudTrueBlack` to the themes API and Night City colour menu; use it for the screen, cached face, ghost pre-blend, and web mirror. Increase only the HUD track/fill to **15 px (1.5x)** and use the same width for invalidation. | Host Night City audit: 517 comparisons / 0 stale mismatches. Firmware built/flashed on COM3; true-black PUT survived restart. Browser mirror/control verified live. Dyno Cell hardware cadence guard remained min 58 / median 61 over 103 samples; Night City reported 44 FPS / 20,191 us worst render with zero TE timeouts after restart. | Keep the default dark face available when unchecked. Any later width change must update static track, live fill, physical dirty bounds, and web mirror together. |
| Night City's immutable readout cache appeared to save little, and its smaller-looking arc still ran slower than Dyno Cell | The source 96 px font is already uncompressed 4-bpp data, so expanding 12 glyphs into A8 PSRAM removes lookup/format work but not the dominant three-pass coverage raster, dirty-box reconstruction, or clipped PSRAM background blit. LVGL charges clipped rectangle area rather than lit-pixel count; Night City also has two ghost passes and a larger 96 px readout, while Dyno Cell has one smaller readout and narrow endpoint work. | Validate every cached descriptor/bound and retain `BOOST_HUD_READOUT_CACHE=0` for matched source-font A/B. Consolidate ghosts/sign/slots into one styleless draw callback with changed-slot invalidation and outer-ring clip rejection. Animate only committed arc geometry through a non-overshooting 40 ms latest-target linear interpolator; raw values still drive numbers, model/API, peaks, colours, and thresholds. | Host 30 s audits: Night City 620/0 stale and 7,297 px/cycle versus 11,254 before; Dyno 620/0. COM3 three-round Night City: gradient-off worstRenderUs 17.06-17.12 ms and 0.47-0.50 Mpx/s versus baseline 18.94-19.08 ms and 0.75-0.78 Mpx/s; median renderFps still 41-42. Dyno guard min 58/median 61 over 103 samples. Region-DBUF-off and TE-off trials did not close the remaining budget; reject the RGB565 tile prototype because host work rose to 21,175 px/cycle. | Do not describe `renderFps` as panel refresh. Optimize `worstRenderUs`, render gaps, over-budget frames, and pixels/s together. Never animate the authoritative numeric/model value or delay overboost colour. Keep draw and invalidation on the same committed visual geometry. |
| Final TPMS/Vault/gradient follow-up | The final branch needed one persisted Vault geometry option, tighter gradient invalidation, and a physical hold that matched the intended feel without weakening the 16 ms gauge path. | Compile-time geometry and repeated same-side full-span invalidation made retained trials hard to reason about; browser Big Digit also used continuous colors while firmware used buckets. | `vaultNeedleTail` defaults off and is persisted through the theme store/API/web editor; draw and all Vault invalidation paths share its 0/26 px runtime extent. Arc/HUD recolor same-side unions once, Big Digit/web share the 24-positive-bucket ramp, and RGB565-equal custom-palette buckets skip needless setters. Hold-to-dim is one second and commits brightness only after a successful panel command. The final 25 s Vault audit measured 10,786 mean px/cycle, zero severe mismatches, and six isolated one-step AA seams over 517 comparisons; simulator and ESP-IDF builds passed and COM3 flashed successfully. | Geometry, invalidation, API persistence, browser parity, and generated assets are one change. Document isolated RGB565 AA seams separately from broad stale trails; never call a strict nonzero audit exit a severe rendering failure when `severe mismatches` is zero. |
| Night City's ring left unused space at the panel edge | The 15 px arc remained at radius 206 and its 16 px ticks ended at radius 214, leaving roughly 17 px beyond their stroke. | Move track/fill radius to 225 and translate unchanged ticks from 198-214 to 217-233; move the zero notch and update physical invalidation plus the web mirror in lockstep. | Host audit: 517 comparisons / 0 stale mismatches. Hardware, one 60 s arm each: gradient off min/median 30/41 FPS, `worstRenderUs` median 19,187.5 us, 0 TE timeouts; gradient on 29/39, 19,889.5 us, 0 timeouts. Dyno Cell gate min 58 / median 60 over 104 samples. | Outer tick strokes deliberately clip at the circular panel boundary. Keep tick length 16 px and all track/fill/invalidation/mirror radii synchronized. |
| Vault-Tec took roughly a second longer than other themes to select | Every scene switch freed its 434 KB static canvas. Returning to Vault synchronously repeated the serial 217k-pixel error-diffusion vignette, including one `sqrtf` and dependent three-channel diffusion per pixel; both swipe and web selection block in the same scene rebuild. | Retain the completed Vault canvas across theme switches and repaint it only when its static-art cache key changes (range, zero angle, palette, face, or vignette). | Hardware web PUT timing after flash: first Vault build 1077 ms; cached returns 64, 101 and 101 ms (other intervening themes 69-170 ms). Host Vault audit: 62 comparisons / 0 stale pixels. Dyno Cell cadence after 3 s settle: min 58 / median 61 over 104 samples. Keep the extra persistent PSRAM allocation in mind when checking allocation failures. |
| Vault-Tec needle briefly appeared at zero after a theme switch | `build_vault()` reset the newly-created needle object to the zero-angle position even though `s_display_psi` retained the latest sample across scene rebuilds. The next 16 ms update corrected it, making the needle visibly jump. | Seed `s_vault_needle_deg` and `s_vault_needle_over` from `s_display_psi` while building the scene. | Simulator audit holds +8.0 PSI, rebuilds into Vault-Tec, pumps LVGL without another sample, and requires the next sample to move the needle by 0.000 degrees before the normal sweep audit. |
| Marquee's single 72-bulb accent border became three zone rings | The user wanted the static dot border to carry the zone mapping: innermost vacuum, middle boost, outermost overboost - and, after a first render, asked for the rings spread wider apart, the centre shrunk to the tube/segments readout (one sprite set, not two), and the old two-tone look (dim neutral track + vibrant accents) restored instead of all-vibrant rings. The old design had 24 live "accent" bulbs restyling on every zone flip; a live-restyling version of three full 72-bulb rings would need 36+ invalidation boxes per flip (a full ring's bounding box is the whole face), overflowing LVGL's 32-slot invalidation buffer and forcing a full-screen repaint on every flip - several times per second near zero crossing. | Bake all three rings statically in the two-tone language: dead bulbs stay `track`, every sixth pair blooms in THAT RING'S zone colour through `neon_bulb_accent()`. No live pass, no flip invalidation; the border only changes with the palette. `neon_bg_key_t` gained `vacuum`/`boost`/`overboost` (marquee only; ring layouts keep them zero) because zone colours are user-editable and are now baked. Rings at centreline 192/208/224 (`NEON_BULB_RING_STEP` 16 = 9 px bulb + 7 px dark gap); outer silhouette unchanged (tips at 228). Marquee's oversized 130 px readout, its marquee-twin cell metrics, its `NEON_MARQUEE_READOUT_TOP`/`STACK_DY` offsets and the `neon_huge` font/sprite set were all deleted - every layout now shares `NEON_BIG`/`NEON_FONT_PX` metrics, one sprite set, one zone-word Y (-95), one lower-stack rhythm; `main/` and `sim/` CMakeLists dropped `neon_huge.c`. The bulb draw stat (`g_neon_bulbs`) was removed with the live pass. | Sim marquee audit: 517 compares / 0 stale / 0 severe, throughput 1.099 Mpx/s - CHEAPER than v1's 1.279 because the smaller shared readout flushes less. Tube and segments audits unchanged at 310/0/0. Parity test re-derives the bulb colour reference from `boost_gauge.c` (dim-then-bloom, matching `scale_rgb` rounding) and asserts the two-tone pattern and the three radii; `node tools/test_neon_web_parity.js` green. Violet and Miami preset renders confirm rings follow the preset's zone colours. | The innermost ring's inner edge is now 188, and the clearance argument is the SAME one the ring layouts already rely on (widest readout ink ~170 vs the ring's 186 inner glow edge) - marquee's bar/PEAK stack sits at the shared rhythm, PEAK corner radius ~185, inside 188. A restyle-on-flip ring on this face is a cadence trap: price the invalidation boxes against LVGL's inv buffer before building one. |
| Marquee rings spread again, centre shrunk to the shared readout, and lighting became a live stage ladder - then a rotating chase | The static all-baked rings read as "too close together" even at step 16; the user asked for ~1.5-2x spread, staggered (not radially aligned) accents, accents that only light once that stage of boost is reached, and confirmed the old two-tone (dim track + vibrant accent) is the look. The outer ring cannot move (tips at 228 vs panel ~233), so spreading moves the inner rings INWARD, which collides with the full-size 118 px readout composition (~184 px half-width incl glow), the bar corners (~172), and the PEAK stack corner (~183) - so the whole marquee centre must shrink, not just accept a tighter ring. | Step 24 (1.5x): rings at 176/200/224, inner edge 172. The centre draws the SAME shared `NEON_BIG` 118 px A8 sprite set scaled to 0.91 (then 0.87) at blit time via `lv_draw_image` scale_x/scale_y (transform_a8 + transform_and_recolor path), with `neon_mq()` scaling every centre geometry constant (readout metrics, bar half/y/h, stack label Ys, invalidation spans) so draw and dirty-boxes agree. `NEON_MARQUEE_CENTER_SCALE` 0.87 + `NEON_MARQUEE_READOUT_LIFT` 12 (extra up-lift because scaling pulls the readout down) shared by draw and invalidation via `neon_readout_top_off()`. Bar shortened 300->264 (`NEON_BAR_HALF` 150->132), lower stack lifted 8 px (unit/rule/peak 128/138/158). Accent pattern staggered per ring `NEON_BULB_IS_ACCENT(i,z) = ((i + 2z) % 6) < 2`; lighting is a CUMULATIVE stage ladder (ring z lit iff `neon_zone_id(psi) >= z`: vacuum lights only the inner ring, boost adds the middle, overboost all three). All 216 bulbs bake in `track`; each reached ring's 24 accents are LIVE-drawn in that ring's zone colour via `neon_bulb_accent()` - a zone flip invalidates only the changed rings' 12 PAIR-boxes each (single-step flip = 12, two-zone jump = 24, inside LVGL's 32-slot buffer). Zone colours were removed from `neon_bg_key_t` again (the bake no longer uses them). Web mirror mirrors the scale, the ladder, and the stagger; parity test re-derives the bulb colour from firmware constants and asserts the ladder at overboost (all three rings lit). | Sim marquee audit: 0 severe, isolated AA seams only (2,577 px / worst 25 px, the known scaled-sprite transform artefact - tube/segments are 0/0), throughput 1.018 Mpx/s at 62.5 Hz. Parity test green including a second render at psi 8.5 to prove all three rings light and the stagger. | **Rotating mode (`neonMarqueeSpin`, persisted per-theme like `hudTrueBlack`):** accent pattern becomes `((i + 2z + phase_z) % 6) < 2` with `phase_z` a 0-5 index, one ring advancing per `NEON_MARQUEE_SPIN_MS`=90 ms tick round-robin (ring z steps when tick%3==z, so a full 6-phase rotation takes 1.62 s), inner/outer clockwise (-1) and middle counterclockwise (+1) on the y-down screen. Phases live in `s_neon_spin_phase[]`, reset to 0 on scene build; unreached rings advance silently (nothing lit to repaint). A spin step changes 2 bulbs per 6-group, so it invalidates 12 three-bulb boxes for the advancing ring - and is SKIPPED on samples that already do a zone flip so flip+spin never stack past the 32-slot buffer. **Bug caught by the spin-on sim audit:** the first version invalidated `base-1,base,base+1` around the POST-advance base, which is correct only for dir=-1; for dir=+1 the accent pair slides the other way and the newly-darkened bulb was left stale (245,772 px severe). Fixed by capturing `base_old` before the advance and starting the 3-residue union at `base_old` for dir=-1 but `base_new` for dir=+1 - spin-on audit then matched the spin-off baseline (0 severe, 2,577 px isolated seams). `--neon-spin` and `--neon-chase DIR` sim flags render the chase for previews; mock server round-trips the toggle; parity test asserts the mirror's `NEON_MARQUEE_SPIN_MS` and direction table equal firmware's and that accent positions follow the round-robin phase formula at explicit tick counts. |
| Marquee dot spacing made uniform across the three rings | The three rings each had 72 bulbs, so the CHORD spacing (2*pi*r/N) scaled with radius: inner 15.4 px, middle 17.4 px, outer 19.5 px - the inner ring was 21% tighter than the outer, which read as cramped. | Per-ring bulb counts chosen so chord spacing is uniform: inner 54 (20.48 px), middle 66 (19.04 px), outer 72 (19.55 px) - within a pixel of the outer's spacing, vs the old 4 px spread. All three stay divisible by 6 so the 2-lit/4-dark accent pattern wraps seamlessly and the invalidation groups of 6 stay whole (`NEON_BULB_N(z)` macro over `NEON_BULB_N_INNER/MID/OUTER`; the spin and zone-flip loops use `NEON_BULB_N(z)/6`, so box counts per ring are 9/11/12). Web mirror (`ringN`), parity test (reads the three defines, ladder + spin + radius blocks all offset-index per ring), mock unchanged. | Parity green (ladder, spin at 6 tick counts, radii); sim marquee spin-on audit 0 severe / 2,578 px isolated AA seams, 1.126 Mpx/s; hardware flash clean boot, toggle survived NVS reboot. Hardware 30 s cadence: median 55-56 renderFps with pixelsPerSecond ~1.1 Mpx/s - the change strictly reduces work (216 -> 192 bulbs, fewer invalidation boxes) and the FPS spread vs the earlier capture is the demo-sweep phase confound, not a regression. | Never assume equal bulb counts give equal visual spacing on concentric rings - the chord scales with radius. Keep the count divisible by the accent period so the pattern and the invalidation groups stay whole. |
| Marquee readout repaints paid LVGL's per-frame transform; pre-scaled sprites fix it | The marquee draws the shared 118 px A8 sprite set at NEON_MARQUEE_CENTER_SCALE (0.87) through `lv_draw_image` scale_x/scale_y, routing every readout cell repaint through the SW transform path (inverse map + bilinear). Host A/B on identical dirty regions isolated the transform at ~35-40% of readout-repaint time: plain blit p50 0.09 ms / max 0.45 ms vs transform p50 0.14 ms / max 2.15 ms, and the per-frame transform's pixel rounding was the cause of the marquee's known "isolated AA seam" stale-pixel class (2,577-2,610 px, never severe). | `neon_bake_scaled_sprites()` bakes a second 0.87-size A8 set ONCE per marquee scene build: each glyph crop is re-rendered through the SAME `lv_draw_image` transform into a scratch RGB565 canvas (so the pixels are identical to what the per-frame draw painted), the exact transformed area is copied out using LVGL's own integer corner math (`bw/2 + ((±bw/2 * scale) >> 8)`), and each slot's `bbox_s` is the tile's dest offset from the blit anchor. Live blits become plain stride-copy blends (scale=1); allocation failure falls back to the per-frame transform. ~100 KB extra PSRAM while the marquee sprite layout is active; first marquee build ~513 ms (scaled bake adds ~66 ms over the plain glyph bake), cached returns ~172 ms. | Host: parity green, geom green, marquee audits spin-off AND spin-on now **0 severe AND 0 stale px** (the pre-scaled blits eliminated the readout AA-seam class), p50 cycle 0.09 ms / max 0.56 ms, big-cycle repaints 0.30-0.44 ms; tube/segments audits unchanged (0/0, 1.258 Mpx/s). Hardware A/B, fresh boot each arm, spin + demo on, pixel shift off, 30 s (bench committed as `tools/bench_neon_readout.py`): BEFORE (per-frame transform) renderFps median 55 / min 41, framesOverBudget/s 33.2, worstRenderUs median 43.0k / max 63.0k us; AFTER (pre-scaled) median 57-58 / min 44-47, over-budget/s **21.6 (−35%)**, worstRenderUs median 42.0k / max 55.7k us. | The median worst stays ~42 ms on both arms: the worst cycles are TE-wait + merged full-region raster bound, not sprite bound - the win shows as fewer over-budget frames and a better tail, not a lower median worst. Any future readout change must keep the scaled-set path pixel-identical to the per-frame transform (same lv_draw_image call at bake) or re-verify the look on glass; never reintroduce per-frame sprite scaling without re-running the host A/B. |
| Marquee accent anchor shifted again; spin-vs-min-FPS measured on the board | The user's static-pattern review of 419d04d found the OUTER ring's accent pair still straddled the bottom axis while the inner ring's pairs flanked it (off-axis), i.e. the two read differently at 6 o'clock. Separately, the pre-scaled-readout A/B (row above) left "why is min renderFps still in the 40s" open: worst seconds had 10-13 over-budget cycles/s with 50-61 ms worst cycles, but the correlation was not isolated. | `NEON_BULB_ACCENT_OFFSET(z)` = `(z)==1 ? 3 : (z)==2 ? 2 : 0` (was 0/3/0): middle ring keeps its bottom-centre pair (bulb N/2 at 6 o'clock, partner one dot left), the OUTER ring moves two bulbs on so its pairs sit at residues 2,3 and FLANK top/bottom instead of straddling them - the inner ring's pattern, as close as the different counts allow (outer pair rel +20/+25 deg vs inner +20/+26.7 at the bottom; the top pair becomes asymmetric as an inherent side effect of the 6-bulb period). Web mirror and parity test (ladder + spin blocks) updated in lockstep; sim marquee audit stays 0 severe / 0 stale. **Min-FPS isolation (fresh captures, same ddbf9d5 build, neon/marquee/demo, pixelShift off; harness `tools/corr_zone_flip_fps.py`):** spin OFF 120 s -> min 58 / med 61, zone-flip seconds never below 58 (flips cost ~2 FPS, ob/s 1.8x steady); spin ON 181 s -> min 45 / med 58, worst 10 seconds carry 10-13 over-budget cycles/s at 50-61 ms worst cycles, 8/10 coinciding with zone crossings. The 40s mins are fast-motion seconds (demo sweep peak slew 9.789 psi/s near the zero crossing), where every tick invalidates the tenths cell + bar span; each spin step is an extra render cycle that pays the regionDBuf TE wait (up to a full 16.7 ms period) when the scan is crossing the readout band (rows ~185-288, right in the scan's path), so ~11 extra cycles/s cost ~13 FPS (58 -> 45). Zone flips add the widest single repaint (~40 k px readout recolor + word + bar + ring boxes, ~40 ms) but are NOT the floor (spin-off flip seconds >= 58). | The spin toggle is the dominant min-FPS lever and it is decorative: each 90 ms step is a render cycle charged the TE wait in fast motion. Caching the accent dots as sprites would NOT help - they are already opaque `lv_draw_rect` fills drawn only where dirty, and the cost is per-cycle (TE wait), not per-pixel; the pre-scaled sprite A/B already proved pixel-cost cuts move min only 41 -> 47. Real levers: slow the spin cadence, defer steps on heavy fast-motion ticks (spin already defers on zone-flip frames), or the stubbed `BOOST_LCD_TE_SCANLINE`. Re-measure min-FPS with `tools/corr_zone_flip_fps.py` (it enables spin, captures ~4 Hz, restores spin to off) rather than a 30 s window - a 30 s window can miss or over-sample the crossing seconds. |
| Neon invalidation tightened for the 60 fps push (2026-08-09 working tree; host-verified, hardware bench pending) | The marquee pre-scaled set AND the full-size set (tube/segments) both blit per-glyph tiles at `anchor + bbox`, but every readout-cell invalidation was the uniform label box (cell pitch + bleed, ~±41 px) unioned with the full-sprite tile — sized to SPACING, not ink, so it over-covered by tens of px per cell on every changed-cell repaint. The marquee bar also invalidated on every 16 ms sample whether the fill moved a pixel or not, and the live accent pass scanned all 192 bulbs on every callback invocation even when the dirty region could not reach the rings. | The invalidation never asked the baked sprite for its own footprint (it has one: the bake-time bbox + tile width). `neon_cell_x_span`/`neon_sign_x_span` now take the glyph index and REPLACE the label box with the tile's exact extent (+1 px AA margin): the marquee scaled set via the pre-scaled tile's `bbox_s` at the scaled anchor, the full-size set (mq=1.0) via the full-size bbox at `spr_dx`. Changed cells union the OLD and NEW glyph footprints (a '1' replacing a '0' sits differently), the count-change path still unions into the full composition box, and the fallbacks (sprites unavailable, marquee per-frame-transform) keep the conservative label-box union. The bar is gated on the DRAWN pixel extent + zone flip (`s_neon_bar_lo/hi`, INT_MIN/INT_MAX sentinels reset at scene build) so a static marquee goes idle like every other face. `neon_bulb_pos` offsets are precomputed once (bit-identical `lroundf(cosf/sinf·r)` table); the live accent scan is skipped when the dirty region stays inside the innermost ring's inner edge (`clip_reaches_radius`). | Host audits (25 s, 1550 samples, all four variants): **0 severe / 0 stale** everywhere. Marquee flush unchanged 14,848 px/cycle (spin 16,441); cycles 1350/1550 no-spin (the bar gate retired 200 cycles) and 1377/1550 spin. Tube/segments flush **19,753 → 18,397 px/cycle (−6.9%)** from the full-size tight span; cycles 1542/1550. `tools/test_neon_geom.c` and `node tools/test_neon_web_parity.js` green. The `±14` pad in `neon_inv_span` is NOT slack — it is the halo band's inner edge (radius 199, width 13 → inner 192.5) versus the invalidation's assumed 228±21 → 207; keep it. | Any future readout change must keep the invalidation and the blit sharing the SAME anchor+bbox formula (marquee: scaled anchor + `bbox_s`; full-size: `spr_dx` + bbox) or the tight span strands pixels. The locked-60 claim still needs the board: re-run `tools/bench_fast_motion.py` sweep + `tools/check_display_cadence.py` per layout with neon/demo on and compare `renderFps`/`framesOverBudget`/`worstRenderUs`, plus a fresh visual tear check. |
| Hardware validation completed (2026-08-09): locked-60 bench and the FPS-vs-TE accounting | The row above left "hardware bench pending". Separately, `_reconcile_te.py` summed waits+skips (~1.47x fps), implying teSkips and teWaits were separate event classes. The real accounting: `te_wait_for_region_spans()` increments `teWaits` UNCONDITIONALLY once per writeback/RENDER_READY-with-data pass (one per render cycle), so **teWaits == renderFps by construction**, and `teSkips` is only the SUBSET of those calls that skipped the wait (skips ⊂ waits). | Verified on hardware across 41 windows: `waits/fps = 1.000` everywhere. Battery A (30 s/window, fresh captures, all four neon layouts + dyno-cell, `teTimeouts 0` everywhere): dyno-cell min 58/med 61 (guard PASS), tube 53/58, segments 40/57, marquee 26/36 — nearly identical to the prior session reference (58/61, 52/58, 39/57, 24/36). Fast-motion constant-slew sweeps (`tools/bench_fast_motion.py sweep`, fresh boot per arm): marquee **min 56 / med 59** (1 ob/s, worstUs 43.4 ms), tube 45/56 (5 ob/s), segments **29/45 (14 ob/s)** — under sustained slew the layout ranking INVERTS vs the organic demo: segments re-lights discrete wedges every tick while the marquee's bar+readout cost stays bounded. Organic cross-check (90 s, marquee): fastest crossing seconds min 43/med 52 vs slowest peak-dwell seconds **min 24 / med 31** — the marquee's floor is the DWELL seconds (needle hovering at a turning point while the tenths digit flutters), not fast motion; the opposite of the Vault pattern, which must therefore be measured per layout, never assumed. `_reconcile_te.py` output fixed to report skips as a % of waits instead of an additive rate. README cadence section updated with these numbers. | A future TE-metric read: teWaits==renderFps is a property of the counter's placement (one increment per writeback pass), not a coincidence; teSkips ⊂ teWaits; only `teTimeouts` is an independent error signal (0 across every run this session). The locked-60 claim is now validated for dyno-cell (guard PASS) and for the marquee in the constant-slew regime (56-59); organic-demo medians sit lower on every face by design. Remaining before calling the neon work done: a fresh visual tear check on the physical panel — framebuffer snapshots cannot contain a tear, so only the user's eyes on the glass can close that item. |
| Contract (2026-08-09): every selectable theme is a locked-60 target; dyno-cell is the reference guard, not a ceiling | The cadence contract and AMOLED gate read as if dyno-cell were the only face expected at 60 FPS ("Only the first of those is the guard" / "the richer faces legitimately sit lower than arc"). That framing predates the other faces — dyno-cell was the only theme when the guard was established — and survived as a permission for every later face to sit below 60. | Per user directive: **every selectable theme locked at 60 FPS** in demo mode — dyno-cell, vault-tec, night-city, big-digit, and neon tube/segments/marquee must each hold a sustained median ≥60 under both the organic demo waveform and the constant-slew fast-motion sweep (`tools/bench_fast_motion.py sweep`, 9.789 psi/s). dyno-cell keeps its status as the reference guard (the conditions the threshold was established under); the sweep is the brutal-test instrument the remaining faces are measured against. No visual compromise is allowed to buy frames; a visual-vs-performance trade is a proposal to the user, not an implementation. | Baseline (2026-08-09, aafcfcf) sweep matrix (constant slew, 9.789 psi/s, fresh boot per arm): dyno-cell min 56 / med 60 (0 ob/s), vault-tec 59/60 (0 ob/s), night-city 41/60 (zone-crossing tail, 77 ob sum over 19 windows), big-digit 57/60 (0 ob/s), neon tube 45/56 (5 ob/s), segments 29/45 (14 ob/s), marquee 56/59 (1 ob/s), marquee spin-on 42/52.5 (8 ob/s). The organic demo differs sharply where dwell-time flutter dominates: big-digit ~27-30 (bucket-boundary jitter re-runs the banded ground sweep), marquee 26/36 (tenths flutter), night-city ~40-42 (zone flips). The two waveforms stress different costs and BOTH are required gates. |
| Theme optimization campaign (2026-08-09): four no-visual-change wins integrated; the remaining gaps are visual-vs-performance trades (proposals) | The locked-60 contract needs per-theme work. A 5-agent campaign (one per theme, each in its own worktree) plus coordinator work produced four host-verified, hardware-verified commits on main. | (a) dyno-cell (`df35958`): `update_arc()` ran a soft-float `%.1f` conversion for the PEAK label every tick and `set_value_arc()` recomputed angles/colours on dwell ticks where the wedge was already committed (its invalidations were zero-length no-ops). Peak text is now cached behind `s_peak_psi`; `set_value_arc()` early-outs on equal endpoint+colour. Wedge invalidation geometry byte-for-byte unchanged. (b) vault-tec (`9402ff2`): `invalidate_vault_needle()` applied one flat 9 px pad to all three radial slices although the wedge tapers 7→2.5 px; pads now follow the local half-width via a new `VAULT_NEEDLE_TIP_HALF` shared with the draw so the two cannot drift. (c) night-city (`ad49e8f`): the HUD fill arc invalidation used a ROUNDED stroke box (over-covered by HUD_ARC_WIDTH/2 on every edge on every tick); switched to the exact flat stroke box + 3 px AA margin. (d) vault-tec (`f0c0c32`, rescued from the completed vault subagent's second commit): `draw_vault_readout()` skips any slot whose box misses `layer->_clip_area` (`neon_area_overlaps`), eliminating the LVGL label draw-task allocation + clip test for invisible slots on every readout repaint; a skipped slot's ink sits inside its own 26x34 box and non-adjacent slot boxes are 23 px apart, so it cannot reach the dirty region. Rendered pixels byte-identical. | Host audits (0 severe everywhere; dyno 0 stale, night-city 0 stale, vault 3-5 isolated 1-step AA seams = the documented baseline condition): vault flushed px/cycle 10786→9926 (−8%, pad fix) then 9926→9862 with the clip-rejection (combined −8.6%); night-city 7141→6658 (−6.8%); dyno flush unchanged (geometry untouched). Hardware (fresh boots): sweep arms (30 s) — dyno 56/60→56/60 flat (the win shows in organic dwell), vault 59/60→58/60 with worstUs 17689→16906 (−4%), then the clip-rejection build (f0c0c32) re-verified on hardware: vault sweep median 60 (min 51 = one settle-transient window), worstUs median 17.9 ms, over-budget median 0, pixels/s 1.06 M, teTimeouts 0 — flat vs baseline, no regression. night-city min 41→43 with over-budget sum 77→70 (−9%, median 60 both). Organic matrix after (20 s arms): dyno 59/60 PASS, night-city 51/62 PASS, vault 52/58, neon tube 54/59, segments 36/56, marquee no-spin 27/34 / spin 25/36, big-digit 21/35 — the known organic floors. Conclusion: dyno/vault/night-city/big-digit all hold median 60 under the brutal sweep; the organic-demo floors (big-digit ~21-27, marquee 25-36, segments 36-56, tube 54-59) are the real remaining gaps, and the safe no-visual-change wins there are exhausted (tight per-glyph spans, pre-scaled sprites, idle gates, segment-box clip, banded recolour all already shipped). | **Proposals for the user (visual-vs-performance trades, NOT implemented):** (1) **Big Digit** — the organic floor is the ground re-colour re-firing on boundary jitter: buckets are ~0.42 psi wide (24 over 0..max) and the demo flutter is ±0.2 psi, so the value sits in boundary-crossing range roughly half the time it is positive; a committed-bucket hysteresis (start a banded wipe only when psi has moved a full bucket past the last committed one) would keep the visible sweep and stop bounce-back restarts, likely recovering most of the 21→60 organic gap. (2) **Neon segments** — the sweep floor (29/45) is the per-cycle raster of ~3.3 segments x 3 arcs plus the readout; reducing `NEON_NSEG` (fewer, wider wedges) is a chunkier look. (3) **Neon marquee** — the dwell floor is the tenths glyph fluttering at turning points; a fast-motion tenths sample-and-hold is a visible readout trade. (4) **Neon spin** — each 90 ms step pays a TE wait (already deferred on zone flips); slowing the cadence trades sparkle for cadence. (5) **Vault peak-mark** (from the vault subagent's report) — the PEAK object is 34x34 while the drawn triangle needs at most ~24x24 incl. AA; shrinking to ~26 cuts ~10% of mean flushed px with byte-identical rendered pixels but changes a layout property, so proposal only. (6) **Vault needle gate** — the 0.35 deg (1.03 px at tip) gate could rise to ~0.5-0.7 deg to skip more sub-pixel repaints; that is a visible jitter/lag trade, proposal only. All are conversations, not shipped. |
| Over-engineering cleanup (2026-08-11): ~2,600 lines of dead code, speculative tools and duplicated harness plumbing removed | A full-tree ponytail audit (not a diff) found dead tool files, a stranded legacy gesture subsystem, an unreachable Sport Cluster renderer, dead model/media plumbing, and eleven copies of the same board HTTP client with drifted timeouts. | **Deleted:** tools/model_neon_demand.py, gauge_preview.html, neon_mockup.py + preview/neon_mock/, bench_neon_cadence.py, neon_sprite_budget.py, capture_state.py; the legacy gesture subsystem + boost_gauge_create() in boost_gauge.c (stranded: boost_page.c owns gesture classification); Sport Cluster outright (renderer, enum member, `"sport"` style token, web mirror - user sign-off; design history stays in git); boost_model media-status plumbing (s_media, set/get_media_status, playback_supported - zero callers); #if 0 neon-numeral block; boost_page_root(); dot_index dead var; web drawSportGauge, mediaPreview, linMap, hudPrevPsi, el.shell/el.gaugeMirror, dead CSS; android dead model fields, pit-lane->dyno-cell defaults, ConnectionStore inlined, dead gradle deps, Math.floorMod. **Refactored:** three theme-payload folds -> applyThemePayload() (web/app.js), three /time POSTs -> syncDeviceClock(), tools/bench_common.py (api_get/put/post/wait_online/wait_display_metrics) shared by bench_fast_motion/bench_region_dbuf/bench_hud_gradient/bench_neon_readout. **Deliberately kept per user:** TPMS protocol layer + boost_tpms get/set/poll/.enabled/BLE define, TE-scanline compile stub, sim run_chase, tools/_reconcile_te.py, RGBA->PNG format. `neonSpinTicks` is NOT dead - the parity test injects it for a deterministic spin phase; the original "dead conditional" finding was wrong and the hook was kept. | Sim rebuilt (boost_gauge/theme/neon_geom/page/tpms all recompiled clean), full IDF build green with regenerated assets, neon web parity green, test_neon_geom + test_tpms_protocol pass, sim screenshots render, all touched tools py_compile, test_map_conversion passes, mock_server CSV endpoint verified, and the refactored dashboard exercised end-to-end in a browser against the mock (cockpit + settings load, badge Live-Http-4Hz, 5 themes/no sport, theme switch to Neon applies, regionDBuf toggle fires its fold). | Guard now: web edits are useless until tools/embed_web.py is re-run (done here); any future re-animation of the marquee spin must keep the state.neonSpinTicks parity-test hook. Next bench work should extend tools/bench_common.py instead of copy-pasting another api_get. |
