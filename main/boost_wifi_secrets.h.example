#pragma once

/*
 * Copy to boost_wifi_secrets.h (gitignored) for STA bring-up:
 *
 *   cp main/boost_wifi_secrets.h.example main/boost_wifi_secrets.h
 *
 * Leave both strings empty to run SoftAP-only (default phone UX).
 */
#define BOOST_WIFI_STA_SSID ""
#define BOOST_WIFI_STA_PASS ""
